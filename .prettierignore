dist
build
coverage
node_modules
.pnpm-store
pnpm-lock.yaml
**/.ssr/
**/*.tsbuildinfo

# Vendored upstream code (see external/agentlet/README.md).
# NOTE: external/agenetes is authored by us and IS formatted (only
# agentlet is vendored), so it is intentionally not ignored here.
external/agentlet/
