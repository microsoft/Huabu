# Web App Architecture (`apps/web/src/`)

> Structure, dependency rules, and conventions for the frontend. The point of
> this doc is the **layering rules** below — not an exhaustive file listing
> (those rot fast; `ls` the dir for the current files).
> Last updated: 2026-09-18

---

## 1. Top-level layout

```
apps/web/src/
├── App.tsx        # Router + WorkspaceGuard
├── main.tsx       # ReactDOM entry
├── index.css      # Global CSS / design tokens
├── pages/         # Production route-level pages & app shell
│   └── playground/ # Development-only visual and interaction test routes
├── components/    # Reusable UI (no business logic): Common, Nodes, Panels, Messages, Milkdown, CodeMirror, Search
├── handler/       # Pure processing logic, no React: canvasCommand/, snap/, pdfHighlight/
├── hooks/         # Shared React hooks
├── store/         # Zustand global state
├── api/           # Backend API clients (one file per endpoint group)
├── i18n/          # i18next setup, locale resources, and translation helpers
├── config/        # Static config, constants, and validated external handbook URL
└── utils/         # Generic utilities (non-React)
```

---

## 2. Dependency rules (the important part)

The web app, docs app, and shared package use the same React 19 runtime and type versions. Keep `react`, `react-dom`, `@types/react`, and `@types/react-dom` aligned across those workspaces because the web TypeScript project compiles shared package source directly.

```
pages → components, handler, hooks, store, api, config, utils
components → hooks, store, utils, config
handler → utils, config, api, store (read-only), other handler modules
hooks → store, api, handler, utils, config
store → api, handler, utils, config
api → config
utils → config   (no React imports except Common re-exports)
config → (leaf — imports nothing internal)
```

**Never import upward** — e.g. `utils/` must not import from `components/` or `pages/`.

---

## 3. Where the canvas engine lives

The canvas-command **executor, command handlers, post-effects, and delta logic
are NOT in the web app** — they were extracted to the shared package
[`packages/shared/src/canvas-engine/`](../../packages/shared/src/canvas-engine) so the
server can run the same engine (headless executor). See
[canvas-command-architecture.md](./canvas-command-architecture.md).

What stays in `apps/web/src/handler/canvasCommand/`:

- `uiIntent.ts` — UI gesture → `CanvasUiIntent` types
- `resolvers/` — resolve ambiguous gestures (selection / clipboard / drag) into commands
- `preprocess.ts` — node preprocessing trigger (calls the server)
- `postEffects.web.ts` — web-only post-commit effects (transition cleanup, deferred frame-fit, history snapshot)
- `nodeInputBuilders.ts`, `utils/` — input assembly + local helpers

---

## 4. Conventions

The main Canvas toolbar marks the active navigation tool or pending node placement with the info foreground and info background, retaining that background on hover. Inactive controls and menu-open states keep their neutral styling. This uses the same selected palette as the touch/pen drawing mode toggles. Its surface shell has 8px horizontal and 6px vertical padding, a 1px edge-default border, 12px corners, and the existing bottom shadow. Navigation, node placement, and undo/redo buttons use 32px square targets with centered 16px icons; shortcut badges sit 1px from the right and 2px from the bottom without shifting the icons. The existing touch/pen drawing mode toggles and compact dropdown triggers retain their own sizing. The 6px button spacing and icon stroke widths are unchanged.

Toolbar numeric inputs commit on Enter or blur. Enter suppresses the following blur commit; Escape cancels the draft without applying a value. Inside a Popover, Escape closes the panel without committing the focused draft, while an outside press blurs focused content before dismissal so width, height, Frame counts, and Question scale edits are not lost when the panel unmounts. Inline font-size inputs follow the same commit/cancel rules.

Production Canvas toolbars use the rounded design shown in the development-only `/playground/node-toolbars` gallery for all twelve current node types. Both reuse Common FloatingToolbar (ToggleButton, NumberInput, ColorPicker, SizePicker), DropdownMenu, Button, the shared accent palette, and SketchControls (including SketchSizePicker). The gallery keeps its own local-state fixtures; production retains real node commands, geometry gestures, history, and visibility gates. Each toolbar starts with one current-type drag control; Text/Note conversion is an explicit overflow command, closing the menu and updating the current-type icon after selection. B/I and numeric font size remain separate controls.

| Node           | Primary controls after type                                                          | Overflow and secondary controls                                                                                                                                           |
| -------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Note           | Accent, SizePicker (including auto/fixed height), expand                             | Convert to Text, move, copy link, delete                                                                                                                                  |
| Text           | Accent, numeric font size (8–160), bold, italic, width-only SizePicker               | Convert to Note, underline, strike, font family, move, copy link, delete                                                                                                  |
| Image          | Accent, SizePicker, expand                                                           | Move, copy link, delete                                                                                                                                                   |
| PDF            | Accent, SizePicker, expand                                                           | Download, remove cover only when present, move, copy link, delete                                                                                                         |
| Office         | Accent, SizePicker, expand                                                           | Download, move, copy link, delete                                                                                                                                         |
| Web            | Accent, SizePicker, expand                                                           | Open original URL only for remote sources, move, copy link, delete                                                                                                        |
| Video          | Accent, SizePicker, expand                                                           | Move, copy link, delete; playback stays inside the node                                                                                                                   |
| Audio          | Accent, SizePicker                                                                   | Move, copy link, delete; playback/recording stay inside the node                                                                                                          |
| Sketch         | SketchControls stroke color/thickness, then SizePicker                               | Move, copy link, delete; no added pen/eraser mode selector                                                                                                                |
| Frame          | Accent, SizePicker with hug/manual, layout panel                                     | Free/row/column/grid as a four-icon segmented control with original tooltip labels and inline row/column counts; no sliders; unframe, move, copy link, delete in overflow |
| Question       | Size button (width and card scale 10–1000% in the same panel), ask/view conversation | Move, copy link, delete; no accent or run/pause action                                                                                                                    |
| Space Shortcut | Accent, automatic/custom width, Open Space                                           | Copy link, delete; no Move, expand, or height control                                                                                                                     |

All node types expose a size button in the primary toolbar, opening SizePicker in a compact, non-modal floating panel using the color picker's shared panel chrome and Floating UI offset/flip/shift positioning. Size and Frame layout panels prefer opening centered below their respective triggers with an 8px gap in both production and the gallery, shifting or flipping when space is insufficient. DropdownMenu forwards an optional `placement` to Popover for floating consumers without changing other menus' default alignment. Layout row/column inputs sit side by side with a fixed 8px gap rather than spreading to opposite edges. The four 32px layout buttons distribute across the panel content width, aligning the first and last buttons with the input row's outer edges. Count tooltips show only the localized field names; input limits and minimum-row semantics are unchanged. Text and Question edit width only. The panel has no backdrop or modal title, follows its trigger, and dismisses on outside press or Escape. Width/height and auto-size behavior remain owned by the existing SizePicker. Text font family is selected through the shared DropdownMenuSubmenu in the overflow menu, with the current choice marked and no independent settings panel.

Common FloatingToolbar.NumberInput uses the shared Tooltip on its value field, with the supplied title or accessible label as its content, instead of a native browser title. Mode controls retain separate tooltips. Its optional `applyUnchanged` and numeric `onApply` return value let Frame pin the displayed row count as a minimum and show the engine's resolved total instead of an unaccepted request; other consumers retain changed-value-only commits. SizePicker accepts `autoSize.togglePosition` and `autoSize.appearance` for the both-axis control, defaulting to `end` and `grouped` for existing consumers. Canvas and gallery use `end` and `separate` for Frame hug: an independent icon follows individually filled W/H fields with 8px gaps, matching the row/column inputs. Note's height-only toggle remains attached to H. Dimension labels remain W/H, while layout counts retain their row/column labels.

Toolbar settings panels (size, layout, color, and stroke thickness) use 8px padding on every side. Color, thickness, and size reuse `FLOATING_TOOLBAR_POPOVER_CLASS` without per-picker padding overrides. Frame layout has an 8px inter-row gap and no additional bottom padding on its count row. Overflow, font-family, and font-size menus use the Common menu defaults: 6px vertical and 4px horizontal padding. These rules apply to production and the gallery; item hit targets and field padding are independent of panel padding.

CanvasFloatingPopover stops double-click bubbling from toolbar controls and their nested portals to the owning React Flow node, without preventing native defaults or child handlers. Common FloatingToolbar.NumberInput selects its entire numeric value on double-click, consistently covering dimensions, font size, and layout counts. Double-clicking these controls never opens a node preview or dismisses the settings panel; double-clicking the node itself retains its existing activation behavior.

Single-node toolbar visibility uses the shared modifier tracker's canvas-specific snapshot: Ctrl/Cmd suppresses chrome only when focus is not owned by an editor or interactive control. This preserves numeric drafts, focus, and native shortcuts inside the toolbar and nested panels; the raw modifier and link-follow snapshots remain unchanged for their other consumers. Returning focus to the canvas while still holding the modifier restores normal suppression.

Common Popover arbitrates Escape by its React nesting hierarchy and opening order: only the topmost dismissible leaf handles a keypress, including nested panels using an explicit portal container. Changing a dismissal callback does not reorder layers. Its dismissal callback distinguishes `escape` from `outside-press`. DropdownMenu and SplitSelect return keyboard-dismissed focus to their trigger after the panel unmounts, avoiding blur commits of cancelled numeric drafts; outside presses retain focus on the user's new target. Nested menu Escape returns to the submenu trigger before a second Escape closes the parent.

`Common/menuStyles.ts` owns list surface, row, label, icon, trailing hint/check, section-label, and separator classes shared by Select, DropdownMenu (including links and submenus), and SplitSelect. Rows use 13px/20px normal-weight muted text, a 32px minimum height, 6px vertical and 8px horizontal padding, 6px corners, 8px content gaps, 14px icons, and shared hover, focus-visible, and disabled treatments. List surfaces use 8px corners with 6px vertical and 4px horizontal padding; Popover still owns background, border, shadow, positioning, and available-space constraints. Selected Select/SplitSelect options retain neutral text with an info-colored check; action menus retain their own current-item semantics and caller-specified danger tone. Section labels are left-aligned 12px/18px subtle text without uppercase, added letter spacing, or decorative horizontal rules. Header action-menu separators reuse the same separator token. No new interaction mode or universal menu component is introduced.

Trigger sizing, selected values/actions, menu width and scroll constraints remain owned by each component or consumer. Select continues matching the trigger width; DropdownMenu and SplitSelect remain content-sized. Multi-field forms, color grids, and toolbar settings retain intentional layout overrides. Production and gallery toolbar lists, including the move destination selector, no longer duplicate default row typography, padding, and hover-radius rules. The `menu-styles` browser regression exercises default components independently of toolbar CSS, including selection versus primary actions, disabled items, links, danger tone, submenus, focus, section labels, and long labels at 320px.

Canvas and gallery overflow menus and Text font submenus use a surface background, 6px vertical and 4px horizontal panel padding, 13px item text, minimum 32px rows, 6px vertical and 8px horizontal item padding, and inset hover backgrounds with 6px corners. Menu rows have no separators or additional inter-group spacing. Overflow, font-family, and font-size menus size to their content without fixed minimum widths. Overflow menus prefer the bottom-left edge of the more button, extending rightward. Both opt into `floating` on DropdownMenu, toolbar ColorPicker and SketchControls: top-level settings prefer opening below their trigger with an 8px offset and viewport flip/shift, follow the trigger, dismiss on Escape or outside press, and do not intercept the first click on another toolbar control with a backdrop. The shared Popover's optional `reference`/`placement` path uses fixed top/left positioning rather than transforms to preserve nested submenu coordinates; coordinate-based consumers retain their defaults. Font submenus retain sideways opening and viewport clamping. Destructive actions retain the danger color. Mobile gallery gutters and production toolbar sizing leave enough room for the full Text toolbar at 320px without reducing button targets.

Common Popover exposes `--popover-available-width` and caps its default width to the viewport or supplied boundary, with `viewportMargin` applied on either positioning path. Coordinate boundaries are intersected with the viewport and update on boundary resize or scroll; anchored panels use Floating UI size middleware. DropdownMenu, its submenus, and SplitSelect size to content and wrap long labels without shrinking icons or shortcut hints. Submenus anchor to their trigger with `right-start` placement and an 8px trigger offset, flipping to the left when necessary instead of clamping over the parent menu. Parent panel padding makes the visible panel-to-panel gap smaller than the trigger offset. Select retains trigger-width matching, capped at 24rem and the available width, and wraps long option labels while descriptions truncate. Caller-specific form, grid, and slider widths remain intentional; callers overriding width limits must preserve the available-space constraint. The browser regression in `apps/web/e2e/popup-width.spec.ts` checks localized content, non-overlapping submenus at both viewport edges, nested actions, long labels, oversized Select triggers, and scrolling coordinate boundaries at desktop and 320px widths.

Text font size combines direct numeric editing and a compact dropdown arrow for common sizes within the existing 8–160 range, without an Aa label or icon. It reuses NumberInput and DropdownMenu, marks the current preset, and keeps bold/italic independent. A divider separates text formatting from node dimensions. The control has a transparent idle background and a shared hover/focus background. All standalone toolbar icon buttons, including Sketch thickness, use 32×32px targets with 6px corners and transparent idle backgrounds; color swatches remain circular, the font-size group shares the same height and corner radius, and the outer toolbar uses 12px corners, matching the main Canvas toolbar. Overflow menus use 14px icons, matching toolbar icons in their 1.65 stroke width and muted foreground for ordinary items; item text is 13px at weight 400, and destructive items retain the danger color.

The gallery displays the shared state rules above the specimens. Ordinary triggers use a temporary neutral hover background and retain that background while their popover is expanded; font size highlights the entire input/arrow group. Color triggers instead retain only the swatch's outer edge-colored ring, with no rectangular background or open-state ring around the button. Keyboard focus indicators remain separate. Text bold/italic buttons indicate selection with the info foreground and a slight increase in Lucide icon stroke width from 2 to 2.25; unselected icons use the muted foreground. Both states retain a transparent idle background and the usual light hover background. Their independent `aria-pressed` states and 32px targets are unchanged. Hug retains its functional emphasis color. Production styles are scoped to node and Canvas context toolbars and their panels, without changing the main Canvas toolbar, drawing settings, or Milkdown toolbars.

Multi-selection, edge selection, stroke selection (including mixed Ink/node selections), and connected-node creation reuse the node toolbar's 12px outer corners and 32px controls through `canvas-context-toolbar`. Multi-selection keeps geometry editing in an anchored settings panel. Pure Text selections expose the same inline TextFontSizePicker as single Text selection: numeric editing, common presets, and localized font-size labels; mixed sizes show an em dash and no selected preset, and mixed node types hide the control. A single command batch applies the size while preserving each node's other style fields. SizePicker retains W/H abbreviations and localizes its width/height tooltips and accessible labels. Its alignment panel has seven actions separated into horizontal, vertical, and spread groups by two 16px-high vertical rules. Edge controls show text-only current values with dropdown chevrons and a color swatch, use content-sized triggers with 32px height, 4px text-to-chevron gaps, 2px inter-control gaps, and 6px horizontal toolbar padding. They reuse compact anchored menus, omit separators, and wrap within narrow viewports. Preview icons appear only in the menus, retaining their actual stroke widths and dash patterns. Stroke selection uses anchored color/thickness pickers and preserves the emphasized submit action, source metadata, and existing selection gates. Connected-node creation retains its creation, keyboard, and tether behavior. These surfaces retain their existing move/delete availability rules.

Fixtures can toggle missing content, PDF cover presence, remote/local Web source, and Question draft/existing/running/pending-fork state. Missing content hides type-specific display/actions but not shared controls. Pending fork hides Question's conversation action. Production owns single-selection visibility, lock/input-mode restrictions, viewport anchoring, and Text/Note conversion guards during ingestion or preview editing; these are not simulated in the gallery. Delete is available in overflow for mouse as well as touch. Expand actions share the same icon while preserving their original labels and preview/conversation handlers.

The gallery is a control-composition study, not a production rendering or command-engine fixture. Drag, conversion, formatting, dimensions, simplified Frame arrangement, cover removal and delete/restore update local specimen state. Question scale is a visual title-size illustration, not the production geometry resolver. Download, external URL and move actions only show local feedback; copied links target gallery anchors, not live nodes. Media is a static specimen and no Agent run is started. Production replacement must retain the existing useTakeoverMarkDrag, geometry/history lifecycle, conditional gates and actual command handlers rather than adopting the specimen callbacks.

1. **Use the `@/` alias** for cross-directory imports (`@/store/canvasStore`); relative paths only within the same subtree.
2. **No `.ts` / `.tsx` extensions** in import paths.
3. **New UI primitives** go in `components/Common/` — check existing ones first (also enforced by [copilot-instructions.md](../../.github/copilot-instructions.md)).
4. **New canvas commands** are added to the shared engine `packages/shared/src/canvas-engine/commands/` + registered in its `index.ts` (`HANDLERS` / `COMMAND_META`), not in the web app.
5. **Shared hooks** belong in `hooks/`; single-component hooks stay co-located.
6. **Barrel exports** (`index.ts`) provide clean import paths in `handler/`, `utils/io/`, `utils/node/`.
7. **Design tokens only** — never raw hex / Tailwind palette / ShadCN aliases. The token declarations in [`apps/web/src/index.css`](../../apps/web/src/index.css) are authoritative; reusable UI contracts live in [`apps/web/src/components/Common/`](../../apps/web/src/components/Common/).
8. **Development playgrounds** belong in `pages/playground/`, use route-level lazy imports, are registered only when `import.meta.env.DEV` is true, and live outside `WorkspaceGuardLayout` so visual testing does not require an active workspace.
9. **Keep the first-screen graph small** — everything statically reachable from `main.tsx` is evaluated before React's first paint, and on the desktop that is the dominant cold-start cost. The canvas route is lazy, the editor toolchain is reached only through dynamic imports, and shared vendor libraries have explicit `manualChunks` homes. See [desktop-startup.md § 3](./desktop-startup.md#3-the-first-screen-bundle-boundary) before adding a static import to anything the app shell reaches.

The development-only `/playground/question-nodes` route provides a store-free Question design study, linked from `/playground/agent-nodes`. Its active `QuestionReferenceGallery` maps eight user references to compact, colored-inset, gray-inset, external-status, tag, notification, rounded-header and divided-header treatments. All reuse `InsetQuestionCard`, AgentAvatarMark, Common Button, the same state fixtures and local preview; no parallel lifecycle is implemented. Each treatment has a synchronized specimen and a collapsible six-state matrix. A separate 280px `compact-notice` hybrid is spotlighted above the gallery: it combines 01's Agent/status metadata row with 06's inset 3px state rail, uses muted 10px status text without an icon or pill, and presents the question underneath in primary text. Original 01 and 06 remain available for comparison. Notification 06 uses a 360px-wide surface (others 280px), an inset 3px state rail, the question as primary text and Agent identity as lower metadata. It omits the visible state heading and icon at every size. Both rail variants retain exact state in the activation button's accessible name and the card's native hover title. Their rails use the existing state tones (draft and viewed share neutral), not six distinct colors. Notification 06 stays on `--bg-surface` in every state and uses an `--edge-default` shadow, with no dismiss action; the hybrid uses a plain bordered surface. Global controls compare state, Agent identity, long questions, theme, five semantic zoom levels and explicitly labeled optional samples. Header large-area color defaults to permission/error emphasis only; a toggle enables all-state color for variants 07–08. Colors use existing system tokens directly, including stronger `*-bg-hover` fills; the colored-inset reference alone retains fixed Question yellow. Single-layer variants remove the nested content border, header-color variants remove the status pill background, and the external variant places its sole status at the upper right outside the shell with Agent identity below the question. Its single activation overlay includes the external badge. No glow, fake progress, decorative conversation icon or redundant default permission sentence is added. All default states omit the detail row. Below 220px screen width, details, Agent name and status words hide; below 100px, the question hides, leaving a 56×32px minimum shell with Agent and status (rail only for notification 06 and the hybrid). External status adds height above that shell. These experimental floors and showcase spacing do not establish dense-canvas collision safety. Opening a preview never grants permission, invokes an agent, acknowledges a real result, or writes to a canvas; formal Question rendering and lifecycle remain unchanged. Previous single-design studies remain in closed historical disclosures.

The hybrid's status text and loading glyph share the rail's `--qi-tone`: running uses info, approval warning, error danger, and an unviewed ended turn success. Draft and viewed remain muted. No status background or font-weight emphasis is added. Running alone adds a 12px loading glyph before the text, including at tiny sizes where the text hides; it reuses the existing `qi-spin` animation and reduced-motion override. This is the exception to the hybrid's otherwise icon-free status treatment, and its tiny shell keeps Agent, state rail and loading glyph within 56×32px. The hybrid and compact 01 abbreviate the ended-turn label to `待查看`, retaining `本轮结束 · 未查看` in the native hover title and accessible name; neither claims successful completion or a new reply. Other variants keep their existing labels. Below the interactive hybrid specimen, an always-visible responsive grid shows all six states at the full 280px reference width, independent of the specimen zoom. It shares the Agent, question, theme and optional sample controls; each card opens its own state in the same local preview. A second always-visible running comparison renders 100%, 75%, 50%, 25% and 10% through the same semantic sizing policy. Unlike the other variants, the hybrid retains status words at 75% and 50% while hiding the Agent name; a shrinkable text span ellipsizes long labels to the available width without squeezing the loading glyph. Only the tiny tier hides both question and status words; 25% and 10% both use the minimum shell rather than proportional scaling. These specimen zoom policies remain playground-only. Production adopts compact 01's visual direction through its own [Question card](./question-node.md), without importing playground components or replacing its lifecycle and collapsed takeover engine.

An independent `QuestionZoomComparison` at `#zoom-comparison` displays only C/D; A/B are retired from the active comparison, with their specimen branches retained for regression coverage. The separate historical gallery and production canvas are unchanged. C (`#zoom-adaptive`) uses an undecorated avatar and separate status in card mode, then a lifecycle ring in far mode. D (`#zoom-rail`) adapts the historical 01 × 06 `compact-notice` fusion: a 3px authored-space left rail and unfilled status text share the lifecycle tone. Its rail is inset 16px from the left and top/bottom; header and title start at 32px, leaving a 13px gap after the rail, while the right inset stays 16px. D measures titles at its actual 352px content width, including while in far mode, and aligns the open tail to the content inset. Avatar, open fill and far-mode geometry reuse C. The rail disappears in far mode and scales without a screen-size floor. Both reuse `resolveNodePresentation` with reading disabled and no node-type override: enter far strictly below 20%, restore the card at 24%, and retain the preceding mode in between, independently of the production Note-specific 25%/28% band and without opting production Question into that policy. Switching is atomic, with no intermediate card squeezing, text fade or title reflow. Each specimen retains one avatar throughout zoom. C/D share a 1–100% slider, ten scale specimens and sixty state/scale specimens, plus page state/title/theme and local Agent color controls. There are no positive minimum sizes for card, avatar, typography, badges or hit target: a single CSS transform scales authored card geometry or the 128px reference far mark (12.8px at 10%, continuing below that). A screen-sized wrapper reserves the transformed footprint in the gallery. Ring proportions reuse the existing size/18 inset and size/12 width without the production helper's minimum clamps. Common Button/RangeSlider, AgentAvatarMark, state fixtures and ring CSS are reused; no canvas lifecycle, profile palette or production takeover behavior changes.

The comparison separates authored layout from viewport scaling. Cards lay out at a fixed 400px canvas width, with 16px insets (D reserves 32px on the left for its rail) and shared canvas-space typography; zoom changes only the outer transform, so name visibility, title line breaks and content height stay unchanged throughout card mode. The flex header uses `useQuestionHeaderFit` to reserve avatar/status and retain the full Agent label when it fits; that decision responds to actual authored-space content, not shrinking screen bounds. `useQuestionTitleHeight` measures unrestricted title wrapping at the authored content width (368px for A/B/C, 352px for D), even while the far mark is active, so returning restores the same content-sized card. Cards fit header, full title, gap and padding with no fixed height or line cap; empty titles add no gap. Both measurements use untransformed computed dimensions, not screen-space bounding rectangles, and ResizeObserver tracks font/content changes and previously collapsed matrix content. Invisible probes are independent of visible fit decisions. Far mode hides the card's text together rather than progressively deleting content while still showing the card.

The historical compact study places status at the top right, keeps a stable conversation/Agent identity label and bubble-shaped avatar enclosure, and omits Note-like body prose. Its historical matrix defaults to this direction. Shared copy uses permission-specific wording and neutral turn-ended wording: `done` may include cancellation and does not prove a new reply, `idle` does not prove a saved draft, and `viewed` does not prove the user read the content. Specific error causes require `errorMessage`; `responseSummary` remains unwritten. The six fixtures do not cover production conflict or pending-fork handling. The page lists these data boundaries rather than inventing progress or outcome details.

The fusion proposal uses text-only near-view status labels and no generic distant status satellites. Only authorization retains a filled warning shield attached to the avatar's upper-right edge, with proportional size and offset. Running uses a solid theme-blue rotating three-quarter arc without a gradient or faint background track; a playground-scoped CSS override retains the shared rotation animation without changing production rings. Unread uses a success ring, error uses the segmented danger ring, viewed uses a quiet neutral ring, and draft has no ring. Full state wording remains in the hover title and activation label at every scale.

An independent local current-open toggle overlays every fixture without changing its lifecycle or acknowledging unread results. Every open state uses the existing theme palette for its `STATUS` tone: running uses `info`, authorization `warning`, unread `success`, and error `danger`. Outlines use the corresponding `*-light` token and fills use `*-bg`, with no local percentage mixing. Neutral draft/viewed use `fg-subtle` outlines and `bg-hover` fills. Closed states retain `bg-surface` (white in light mode and the corresponding dark surface). The card, avatar disk, speech tail and far bubble share `--qzc-shell-fill`; the card outline, tail and bubble stroke share `--qzc-shell-edge`. D's rail and status text retain the unmodified status tone. Open cards use a 2px authored-space inset outline, a larger 32×20 speech tail, and higher-contrast current-open metadata. These changes do not affect content geometry or text metrics. C/D reuse `QuestionAgentBubble` with a larger proportional enclosure and a 3px viewBox-space `--qzc-shell-edge` stroke, replacing rather than stacking with the lifecycle ring in every open state, including authorization. Authorization retains its upper-right shield without suppressing the open bubble or tinted fill. Open far marks intentionally omit the running arc; running remains indicated by the near card's status. C/D switch from card/tail to the bubble together with the far-mode change. All chrome scales with the node without a screen-size floor. Full activation/hover labels include both lifecycle and current-open. The fixture does not imply that production leaves results unread while actively viewing them. Keyboard focus retains its normal focus-visible outline, independently of current-open. Clicking a specimen only opens the existing local preview.

The global light status palette in `apps/web/src/index.css` adopts the harmonized comparison colors. In main/outline/background/hover order, warning uses `#D97A2B` / `#E2B38D` / `#FDF6F1` / `#F7E5D6`, success uses `#1B5E20` / `#A4C9A4` / `#F3F9F3` / `#DEEEDD`, and danger uses `#B82132` / `#E3B2A9` / `#FCF8F8` / `#FBE2E1`. Blue, neutral, warning main, and danger background stay unchanged, as does the complete production dark palette; experimental dark OKLCH colors are not adopted. `QuestionZoomComparison` defaults to inheriting production tokens without duplicating the new palette locally. Its `配色：新版` / `配色：原主题` toggle compares the adopted palette with scoped former light-theme values; both modes inherit production dark colors. Other consumers, including historical galleries, receive the global light update through existing semantic tokens. Specimens continue consuming `*-light` / `*-bg` / `*-bg-hover` without local color mixing.

The comparison previews current-open, not canvas selection. It has no selection toggle or selection-colored overlay; opening a conversation and selecting its canvas node remain separate concepts.

The comparison defaults to the supplied Copilot artwork through a local `CopilotPreviewIcon` sourced from MIT-licensed Primer Octicons, with attribution retained in the component. The `Copilot 图标` toggle restores the existing Agent avatar for comparison. Copilot artwork is centered at 72% of the unchanged avatar slot to leave space inside status rings and the open bubble; other Agent artwork retains its existing size. Artwork uses the theme's foreground color for light/dark contrast. Running rings use `info` in both themes, not Agent identity colors; other lifecycle colors and current-open chrome stay unchanged. This is a playground-only visual override, not a new persisted Agent icon type or production avatar replacement.

Shared canvas styling lives in [`Nodes/design/`](../../apps/web/src/components/Nodes/design/): accent tokens, node size tiers and radii, boundary width and colors, reference-width spacing, and typography, with matching tests colocated. [`Nodes/previewCard/`](../../apps/web/src/components/Nodes/previewCard/) owns the PDF/Web `PreviewCard` component, CSS, tests, and dedicated `previewCardDesign.ts` metrics. Preview-card design consumes shared design, never the reverse in production modules. Frame and Note retain local `frame/frameDesign.ts` and `note/noteDesign.ts`; component CSS stays with its component rather than moving into shared design.

Only Image, Video, and Text offer No accent: their resting surfaces can be genuinely transparent, and unaccented Image/Video omit the permanent media boundary while retaining interaction chrome. Note, PDF, Web, Office, Audio, Space Preview, and Frame have intrinsic card or structural surfaces, omit No accent, and render missing legacy accents as White without a data migration. Sketch uses stroke colors rather than node accents, while Question uses its own semantic surface. PDF/Web overview and minimal modes retain their accent card; reading mode removes the outer fill, boundary, and elevation so the document or page supplies its own edge.

Node-only far zoom uses production [`Nodes/semanticZoom/FarZoomLabel.tsx`](../../apps/web/src/components/Nodes/semanticZoom/FarZoomLabel.tsx) and [`FarZoomText.tsx`](../../apps/web/src/components/Nodes/semanticZoom/FarZoomText.tsx), with screen-pixel metrics and description line allocation in [`Nodes/design/farZoomDesign.ts`](../../apps/web/src/components/Nodes/design/farZoomDesign.ts). The playground imports these shared implementations rather than owning a parallel renderer. Note/Office use the text-only `SemanticPlaceholder`; PDF/Web retain `PreviewCard` and its image/orientation, with screen-fixed title/description metrics from the same design configuration. Web omits the favicon/hostname row and its title gap only in far mode. Title-first fitting removes descriptions that do not fit; images use the remaining flex area. Card-local inverse scaling preserves actual glyph metrics without changing node geometry. Office gains no reader. Frames use the independent region-name policy described below.

`useNodePresentation` and `semanticZoom.ts` own the production policy. Notes enter minimal strictly below 25% viewport zoom and restore ordinary content at 28% or above through a Note-only `minimalZoomByType` override. PDF, Web, Office and Video retain the default 20%/24% band. The hook passes the node type to the shared resolver; authored node dimensions do not affect activation. Initial presentation history is overview; subsequent changes retain the preceding mode inside the band. Screen bounds determine far-text fit, not minimal activation. Existing reading thresholds stay at 560 × 360px to enter and below 480px width or 300px height to leave, with the same viewport lifecycle, sole-selection interaction, and authored-interactive-view exclusions. Full/minimal visibility switches immediately with no cross-fade or text fade; hidden bodies are inert and accessibility-hidden. Playground callers that omit the node type retain the default band. See [canvas-zoom-rendering.md](./canvas-zoom-rendering.md#3-node-level-of-detail) for the complete contract.

Note/Office supply their resolved title and description through `farLabel`; PDF/Web pass existing metadata directly to their retained cards. The label renderer neither fetches metadata nor generates summaries. Note reuses `noteFarDescription` and shared `stripMarkdown` to select the first useful prose paragraph, excluding headings, code, lists, image captions and title duplicates; no separate description is stored. Existing hydration gates remain intact: initially minimal Notes defer editors and retain them once hydrated, while PDF thumbnail work and Web preview requests retain their existing presentation gates. Far-zoom text introduces no additional fetching, editor mount, or Markdown parser. Missing media in minimal mode uses the text-card fallback without a deferred loading skeleton.

The development-only `/playground/design` route is the visual reference for canvas component design. Its Frame section renders the same store-free `FrameSurface` and `FrameHeader` presentation components used by `NodeWrapper` / `FrameNode`, with production [`FRAME_DESIGN_CONFIG`](../../apps/web/src/components/Nodes/frame/frameDesign.ts), responsive resolution, semantic tokens, and accent palette; only the simulated child content is playground-owned. It demonstrates Frame tiers, authored dimensions, surface accents, header kinds, and all four layout modes. The Note section renders scroll-enabled, read-only S/M/L specimens through the production `MilkdownPreview`, `NoteContentViewport`, and `NoteTruncationOverlay`, without canvas-store writes. Its geometry-tiered 12 / 18 / 24px radius and fixed 3px boundary reuse `design/nodeDesign.ts` and `design/nodeBoundary.ts`, independently of preview-card design; neutral surfaces, accent boundaries, 16px inline and block reference insets, and lifecycle-managed first-block outer-inset rules are also production behavior. Zoomed comparisons use actual screen dimensions so a proposal cannot appear successful only because it is inspected at authored canvas size.

The `#zoomed-overview` section contains `ZoomReadabilityStudy`, a side-by-side comparison of original scaling and the shared production node far-zoom and Frame-region policies, plus a local Frame header readability experiment. The former mini-card layout remains removed. Shared Button presets and RangeSlider expose 5–100% zoom, initially 50%: the 400 × 320px nodes remain ordinary Note documents and Web/PDF cards at 200 × 160px on screen. Four contexts use identical geometry on both sides: independent nodes, short-title 400 × 700px tall nodes, ordinary nodes within a Frame, and two dense Frames with six 240 × 180px nodes each. Existing `NoteSpecimen`, `PreviewCard`, Frame surfaces, and header metrics are reused; the Web image is sample artwork and PDF uses the existing no-cover card. Tall fixtures use shortened titles of the same source examples, not invented summaries of other products. There are no screenshot requests or canvas-store writes, and the study does not demonstrate Office or reader activation.

Shared `FarZoomLabel` and far `PreviewCard` use fixed 12px / 16px title typography at weight 500, matching Frame region titles, plus regular-weight 10px / 16px descriptions after a 2px gap. All far descriptions require room for at least two complete lines after the full title; normal cards retain their existing typography and one-line minimum. Vertical far preview cards reserve the existing quarter-inner-width cover budget before allocating title and summary lines, while horizontal and text-only cards retain their information-area budgets. Far card titles use every complete line that fits, with no fixed two-line cap. Covers, orientations, summary caps and normal-view title clamping remain intact. Text-only labels have up to 6px horizontal and vertical screen insets inside the 3px canvas-space shell boundary; horizontal insets shrink to preserve one em of title width, and vertical insets shrink to preserve one complete title line before hiding it. The renderer uses the same computed insets and typography as fit detection. One em is a geometry budget, not a measurement or guarantee of a particular glyph's ink bounds. CSS owns language-aware wrapping, emergency word breaks, clamping and clipping, with no separate fixed outer-width retention band. A hidden, accessibility-excluded full-title probe measures the title after resize and font loading, capped to the displayed title's line budget. Once the minimum fits, text-only labels use every remaining complete description line, without a fixed maximum count; absent or title-identical descriptions add no row. Descriptions use `text-fg-muted`, never fractional opacity or a fade. Hidden labels skip measurement and font observation; visible labels share one font-change subscription. The playground consumes the shared defaults without a separate specimen redesign. Neither node heights nor Frame layouts are compressed.

Playground `farZoomPresentation.ts` delegates node content selection and label fit to production `resolveNodePresentation` and `resolveFarLabelLayout`, passing actual viewport zoom and reusing the 20%/24% band and overview initial state rather than maintaining independent thresholds. Its separate Frame header experiment remains local: names use at least 14px text, stay left-aligned above children, and hide below 64px available width or when a complete line cannot fit; restoring them requires 80px width and 2px spare header height. The header experiment uses the midpoint of its entry/exit bands initially; the shared region takeover starts from normal presentation, matching production. Component-local state carries the study's hysteresis without canvas-store writes. Original positions, shell colors, and node silhouettes persist; hidden specimen bodies stay mounted, inert, and accessibility-hidden. A visible header alone does not suppress grouped labels. The header experiment's budgets are not production header policy or validated readability limits; the region takeover below is shared with production.

Within `#zoomed-overview`, `FarLabelTypographyComparison` provides the direct anchor `#far-label-typography`. Paired long-Chinese-title, short-English-title, narrow, and shallow Note fixtures use identical content and geometry with production boundary, radius, accent, and Note surface tokens. A synchronized 5–50% slider starts at 24%. Both sides intentionally isolate the far-label layer, not content switching or Frame takeover. The left preserves historical metrics (title weight 500, description 9px/12px, horizontal inset 6px); the right uses the adopted production metrics (shared title weight 600, title 9px/13px, description 7px/10px, horizontal inset 6px). Vertical title-priority insets remain unchanged. The canonical `FarZoomLabel`, `resolveFarLabelLayout`, and description allocator accept optional design metrics; omitted metrics use the adopted production defaults. No footer, shadow, new surface palette, or canvas-store writes are introduced.

The grouped and dense study fixtures derive their outer bounds and child spacing together through the shared `resolveFrameResponsiveLayout` Hug Frame tier resolver, then use `frameVisualMetricsForSize` for appearance and the resolved insets for `getFrameHeaderMetrics`. Width includes three child columns, two gaps, and equal left/right padding; height includes the child rows, intervening gaps, top header inset, and bottom content padding. The grouped Frame is 1280 × 404px and each dense Frame is 800 × 464px, with 64px top inset and equal 20px left/right/bottom padding. Scene bounds follow these computed Frames; the two dense Frames retain their 80px inter-Frame gap. Neither the former synthetic 220px header band nor fixed oversized right/bottom extents remain. Zoom never moves children or resizes Frames to accommodate the name. These fixtures represent content-hugging responsive layout, not a guarantee of spare space in manually authored Frames, whose actual child positions determine their header space.

The study and production share the centered region-name takeover through `frame/frameZoom.ts` and `FrameRegionLabel`. Eligibility enters strictly below 10% viewport zoom and releases at 12% or above, retaining the preceding state between those boundaries, independently of ordinary child sizes and after the node minimal threshold. Nested handoff uses minimum label fit rather than a separate zoom threshold: when an eligible inner branch is below the preferred 28px × 49px screen budget, the nearest preferred-fit ancestor replaces its whole subtree atomically. Readable branches remain at the deepest usable level; a failing sibling promotes their common parent. Recovery uses 2px spare width and height. If no preferred-fit ancestor can replace a branch, the best-effort fallback keeps a region whenever its content box has positive width and at least one complete line, allowing CSS to expose partial information rather than leaving a blank shell. The name uses fixed 12px / 16px screen typography, and the trailing direct-child count badge uses 7px numerals; no fixed line cap, glow, blur, or shadow is added. Visible region labels replace original headers and descendant content beneath a 60%-opacity Frame-surface veil. Empty Frames remain unchanged. One Canvas-scoped controller derives ephemeral ownership; no canvas/history writes or geometry changes occur. See [canvas-zoom-rendering.md §4](./canvas-zoom-rendering.md#4-frame-and-frame-label-policy).

Production `FarZoomText` is shared by the measured title probe, visible node titles, descriptions, and playground region names. Normal inline flow preserves fitting words at wrap boundaries; `overflow-wrap: anywhere` permits internal breaks only for overwide words. Tiny non-wrapping groups attach opening punctuation to the first character and trailing punctuation or spaced separators to the final character. There is no per-word ellipsis or automatic hyphen insertion; only the containing line clamp truncates at its final visible line. CJK retains normal line breaking. Full source text remains in the DOM; description line allocation uses the identically wrapped title probe and all remaining complete lines. This wrapping applies to the shared far-zoom layer, not normal document or overview-card text.

The same route includes a store-free selection-frame proposal at `#selection-frame`, implemented by `SelectionFramePlayground.tsx`. Only selected specimens expose four connection dots, each 24 screen pixels beyond its corresponding edge. Their visual treatment follows `NodeConnectAffordance`: information-color solid dots (8px mouse / 14px coarse pointer), growing to 20px / 22px with an inverse-color plus on hover, keyboard focus, or a pending connection. Dot centers and outward-expanded hit areas (20px / 28px) remain fixed, separated from resize handles; there is no arrow button shell or decorative stem. All ports unmount during pointer resizing and return on release, Escape, pointer cancellation, or lost resize capture. Selected nodes and their controls form a raised stacking context above other specimens; pointer capture retains gesture ownership. The Overlap test preset exercises this without blocking unrelated empty canvas. Pointer dragging or click-then-target creates illustrative links from the chosen source side; unlike production, the study does not implement click-to-create. Image specimens preserve aspect ratio at four corners, Free text exposes only left/right width handles with separate font-size and content controls and content-driven height, and cards use free-axis corner resizing. Explicit resize cursors override generic button styling and persist across the study during dragging. Controls retain screen-space sizes at 50%, 75%, and 100% zoom. Resize controls also accept arrow keys. This is an interaction study, not production React Flow routing, persistence, undo, collision handling, or a replacement for existing node controls.

Production Note/Office use the shared far-label renderer, PDF/Web retain their image cards with the same far typography tokens and zoom boundary, and Frames use the shared region-name takeover with descendant-shell dimming. Frame layout, geometry, colors, and ordinary header styling remain unchanged. The selection-frame proposal and the playground's separate header readability experiment are not production behavior.

The historical Note surface hierarchy comparison and its local tinted-surface override have been removed. The remaining Compact, Regular, and Large Note specimens and the current zoom study use the production neutral surface with accent borders. This playground cleanup does not change production Note surfaces or color behavior.

The page orders Frame styling, Note styling, the current zoom-readability comparison, and the selection-frame proposal. The earlier independent Frame overview experiment and its implementation/tests have been removed rather than retained in a collapsed disclosure; the current study demonstrates the adopted Frame region-name takeover. Full Note document truncation uses the production noninteractive fade, with no footer or `Show more` action; far-zoom title and description text never fades. The selection-frame proposal does not change production canvas behavior.

Space Shortcut retains the `spacePreview` node type and renders the canonical icon, current target title, and a muted node-count/update-time summary using shared node chrome. It reads shared workspace metadata, never a target scene or nested React Flow, and supports bounded automatic width and minimum-only custom width with content-owned height. See [space-preview.md](./space-preview.md).

### External Agent Settings

External Agents Settings uses `components/Settings/agent-profiles/` and the canonical `acpProfilesStore`. It has ordinary command Profile creation/editing/deletion and no Agent Team templates, Configs or setup lifecycle. The manual editor reads the agentlet-backed catalogue; automatic defaults arrive through the same persisted Profile list used by selectors. See [Agent Profiles](./agent-profiles.md).

### Toast duration contract

[`Toast`](../../apps/web/src/components/Common/Toast.tsx) derives its default auto-dismiss duration from tone: `danger` toasts persist until dismissed, while `neutral`, `info`, `success`, and `warning` toasts dismiss after 3000 ms. Callers may explicitly set `duration` to override either default, including a positive duration for a transient danger message or `0` for any persistent message.

### Assistant tool disclosures

Generic tool calls render through [`ToolCallCard`](../../apps/web/src/components/Messages/AIMessage/Tool/ToolCallCard.tsx) and start collapsed by default. A completed textual `task_complete` result is projected into an ordinary top-level assistant text segment before phase grouping, so it closes any preceding thinking phase, renders as Markdown, and participates in Copy and Add as note. ACP tool updates are cumulative, so the latest text content block is authoritative; a pending, failed, or textless `task_complete` result remains a generic tool card.

### Clipboard contract

Canvas copy carries Huabu's serialized node payload so that pasting back into Huabu preserves node identity and artifact ownership. The payload always rides in `text/html`; the other representations exist for applications outside Huabu:

`Cmd/Ctrl+C` preserves the browser's native copy behavior when the user has an actual text selection in an input, editor, preview, or panel. If an editor merely retains focus with a collapsed caret, the shortcut copies the selected Canvas nodes instead; retained editor focus must not turn node copy into a silent no-op.

| Selection              | `text/plain`                       | `text/html`                                                  | `image/png` |
| ---------------------- | ---------------------------------- | ------------------------------------------------------------ | ----------- |
| Exactly one image node | —                                  | `<img src="data:…" data-huabu-nodes="<base64 payload>">`     | the image   |
| Anything else          | readable text (omitted when empty) | `<span data-huabu-nodes="<base64 payload>">same text</span>` | —           |

The payload moved out of `text/plain` because plain-text targets outside Huabu would otherwise receive raw JSON. `text/html` was chosen over a `web ` custom format because custom formats are Chromium-only on the read side, while `DataTransfer.getData('text/html')` works synchronously in every browser — which matters because all paste entry points are synchronous `paste` handlers. The image is inlined as a `data:` URL rather than linked by artifact URL because rich-text targets often prefer `text/html` over `image/png` and cannot reach Huabu's local API origin. A single image node writes no `text/plain` at all, so pasting it outside Huabu produces an image and nothing else.

The readable text is produced by [`nodesToPlainText`](../../apps/web/src/utils/io/nodeToPlainText.ts): `note` / `text` / `question` paste their `content`, `web` pastes its `src`, file-backed nodes, Frames, and Space Previews paste their label, and `image` / `sketch` contribute nothing. Multi-node selections join the non-empty parts with a blank line. Non-image copies repeat that same text inside the `text/html` element so rich-text targets do not paste an empty element; newlines become `<br>` there, because rich-text targets prefer `text/html` and HTML would otherwise collapse a multi-line note into one line.

All representations are written through a single `ClipboardItem` by `copyCanvasClipboard`. One copy gesture only authorizes one clipboard write, so writing `text/plain` first as a safety net makes the real write fail with `NotAllowedError` and silently drops the image.

Read paths must go through `readHuabuClipboardPayload` (or `readHuabuClipboardPayloadAsync` on the Clipboard-API fallback path), which prefers `text/html` and falls back to `text/plain`. The fallback keeps clipboard contents written by older builds working. Unsupported Clipboard APIs, inaccessible sources, and image conversion failures all fall back to writing the serialized node payload to `text/plain`, which degrades external pastes to JSON but keeps Huabu-to-Huabu paste working.

The Image expanded preview header exposes explicit Copy image and Download image content actions through the shared `PreviewHeaderSlot`; the Canvas node toolbar retains only node-level actions such as Open Large View. Copy image writes pixels only through `copyImageToClipboard`: PNG bytes pass through, while other browser-decodable raster or vector sources are rendered to PNG with `createImageBitmap` and an `HTMLImageElement` fallback. Clipboard API unavailability, permission denial, source fetch failure, or decode/encode failure produces a persistent error toast with a Download image action instead of silently reporting success. This action is distinct from Canvas `Cmd/Ctrl+C`, which must continue carrying the Huabu node payload for round-trip paste.

Image asset identity is separate from displayed geometry. `data.src` identifies the original uploaded, generated, inline, or remote source; top-level `style.width` and `style.height` only control its Canvas rectangle and never resample or replace that source. Explicit snapshot operations may create internal transformed PNG artifacts for agent vision, but ordinary geometry resize does not create image variants.

### Cross-canvas paste and artifact ownership

Artifacts are canvas-owned, so a paste into a different Canvas than the one the payload was copied from must clone every referenced file into the destination before dispatching `PASTE_CLIPBOARD`; otherwise deleting the source Canvas would orphan the pasted node. A node references artifacts two ways, and `pasteNodes` walks both from the same shared source of truth in [`artifact-url.ts`](../../packages/shared/src/utils/artifact-url.ts):

| Reference shape                 | Field source             | Rewrite                                                    |
| ------------------------------- | ------------------------ | ---------------------------------------------------------- |
| Dedicated top-level field       | `ARTIFACT_DATA_FIELDS`   | field value replaced with the cloned key                   |
| Image embedded in Markdown body | `markdownArtifactFields` | each `![…](<key>)` destination rewritten to the cloned key |

The Markdown walk is what keeps images inside a `note` alive across Canvases — they live in the body string, not in `data.src`. It is scoped by node type (`markdownArtifactFields`) because `content` also exists on `text` and `question` nodes, whose bodies are plain prose that must not be rewritten. `parseArtifactRef` decides what is cloneable: bare keys and legacy canvas-scoped URLs are, while `data:`, `blob:`, and external `http(s)` sources are left verbatim. Clones are deduplicated per `(source canvas, key)` for the whole paste, and a failed clone falls back to the original key so the node renders the server's missing-artifact placeholder instead of blocking the paste. Because the clone round-trips are async, the result is dropped if the user has navigated to a different Canvas by the time they settle. Same-canvas pastes keep sharing the original artifact and stay on the synchronous fast path.

### Moving a selection between Spaces

Move is an explicit server-coordinated action rather than a clipboard operation. The single-node and multi-selection floating toolbars open one Canvas-level `MoveSelectionPopover`, anchored to the single-node overflow trigger or the multi-selection Move button. It reuses Common Popover and `FLOATING_TOOLBAR_POPOVER_CLASS`, with no backdrop. Unlike the smaller toolbar pickers, this multi-field form overrides the shared padding to 12px and uses 12px gaps between its heading, destination fields, shortcut option, and confirmation group. The heading-to-description gap is 4px; destination fields and confirmation contents use 8px gaps. Main text is 13px with 20px line height, auxiliary descriptions are 12px with 18px line height, and Select, TextInput, and action buttons retain matching 32px heights. The production UI and `/playground/node-toolbars` share `MoveSelectionPanel`; the gallery only reports a local preview and never moves real content. The panel lets the user choose an existing ordinary Space or enter the name of a new Space, and provides a default-enabled checkbox for leaving a source `spacePreview` breadcrumb. Form state resets on each opening, the selected node ids are captured when opened, and the Frame-descendant notice appears only for a selection containing Frames. Submission remains explicit, with the shortened boundary-edge removal notice directly above the confirmation buttons rather than between the fields and shortcut option. Escape dismisses the nested destination menu before the move panel; outside press dismisses without a blocking overlay, and keyboard dismissal returns focus to the trigger. Submission disables controls and dismissal. The controller drains pending Canvas saves and submits the captured node ids, Preview choice, and current source version to `POST /api/canvas/:canvasId/move-selection`. A successful toast reports the durable outcome and links to the destination; typed server failures are shown without applying optimistic local mutations. `spacePreview` nodes do not expose the action.

The move panel uses Common Popover's optional `onOpenAutoFocus` callback to focus its first control only after positioning makes the panel visible; other popovers retain their existing focus behavior. While the move panel is open, the single-node toolbar stays mounted even when Ctrl/Cmd (including Ctrl+Shift) is held, preserving the live trigger anchor during text-editing shortcuts. Once the panel closes, the toolbar's normal modifier-based hiding resumes. Common Select returns focus to its trigger after a choice or keyboard dismissal and accepts optional `menuClassName` styling without changing its default dimensions. Switching source Space or workspace closes the move panel, and a context change while draining saves prevents the request from being sent with another Space's version.

The destination list separates the Create new Space option from existing Spaces with Common Select's `separatorBefore` option and the shared menu separator style, without a redundant New destination heading. Other Select consumers can still use named sections.

Common Popover observes outside pointer presses in the document capture phase so canvas gesture handlers cannot swallow dismissal; it does not consume the event, preserving normal canvas selection and navigation. Before submission, clearing or changing the selected node IDs also closes the move panel, keeping it tied to its toolbar selection even for non-pointer changes. Geometry and data updates with unchanged selected IDs preserve the form. During submission, selection changes do not dismiss the panel or alter the captured request.

Move failures use an allowlisted `ApiError.code` mapping to localized English/Chinese text in a persistent danger toast, leaving the panel open. The panel never renders server messages or details, including unknown codes, network errors, and pending-save failures. Ambiguous outcomes, compensation/cleanup failures, and unknown failures instruct the user to reload and reconcile both Spaces before retrying; the panel does not automatically resubmit.

## Workspace routes and World

`/` is the workspace landing redirect. When the persisted World setting is enabled it redirects to the hidden World through `/canvas/:worldCanvasId`; otherwise it redirects to `/spaces`. The ordinary Space List remains a sibling page at `/spaces`, and every Canvas scope, including World, continues to use the existing `CanvasPage` and `/canvas/:canvasId` route.

The canonical shareable Node deep link is `/canvas/:canvasId?node=:nodeId`. Both identities are stable IDs rather than labels; the builder encodes the Canvas path segment and emits only the `node` query, while the parser rejects empty, duplicate, malformed, and overlong Node values. The URL is presentation state owned by a React-free web utility and `CanvasPage`, not a Canvas command or server wire contract. Copy link is available from the single-selected-node floating toolbar and uses the shared clipboard helper plus localized accessible toast feedback.

`CanvasPage` consumes a deep-link intent only after the route Canvas, authoritative Canvas payload, matching Preview Workspace, and React Flow instance are hydrated. It exits Preview fullscreen first when necessary, resolves the target exclusively from the authorized Canvas payload, selects it through `canvasStore`, and focuses it through the shared reliable-bounds viewport helper, which supports offscreen nodes and nested Frames. The query remains in the address bar: rerenders and later hydration do not replay a consumed activation, refresh consumes it in the new runtime, and leaving then returning through browser back/forward consumes it for that route activation. Visiting an ordinary Space URL clears the prior activation marker. The intent is marked consumed before side effects so synchronous selection and panel changes cannot re-enter it.

Missing, deleted, malformed, unavailable, or unauthorized targets all preserve normal Space loading and produce the same generic localized unavailable status. Target resolution performs no node-by-ID request and exposes no label, type, existence distinction, or other metadata. A recognized node without an expanded renderer is still selected and focused and receives a generic informational status.

Primary navigation between the Space List and an ordinary Space uses React Router links rather than button-owned `navigate(...)` calls. An ordinary click therefore stays within the current tab and passes through the router's pending-save blocker, while Ctrl/Cmd-click, middle-click, keyboard activation, assistive-technology link discovery, and browser context-menu actions retain native anchor behavior. The Space Preview interaction model is intentionally separate from this list-to-Space contract.

The World setting defaults to disabled. Enabling it exposes the World navigation entry and changes subsequent workspace landing to World without deleting or resetting `.world`.

World renders managed `spacePreview` nodes through the same compact shortcut renderer as ordinary Spaces. Target names and availability come from workspace metadata. Double click, selected-node Enter, and toolbar Open Space enter the target; no local scene pan/zoom exists. Managed membership and target identity remain server-owned. See [space-preview.md](./space-preview.md).

The Portal/Pin node renderers, reference endpoint client, `worldReferences` and `pinnedSourceNodeIds` stores, Pin/Unpin toolbar actions, and reference refresh lifecycle are removed. Ordinary Preview Workspace tab Pin actions remain: they promote a transient tab and do not create World topology.

Canvas load, clipboard preparation/resolution, and undo/redo restore reuse `stripLegacyPortalTopology()` to ignore retired nodes and incident edges while preserving and rebasing ordinary children. Load applies the filtered state without autosave, disk cleanup, or legacy ID/geometry migration. A later ordinary save may omit the ignored data; see [canvas-storage.md](./canvas-storage.md#retired-portalpin-topology-on-load).

Question tabs resolve only ordinary live nodes in the active Canvas. `PreviewRenderer` still builds an explicit `ChatSession` with `AgentConversationView`, but presentation and owner must identify the same Canvas/node and the live thread must match. There is no World `nodeRef` source-conversation renderer; entering a source Space precedes opening its Question conversation.

The web keeps undo/redo managers in a registry keyed by `canvasId` while retaining one active Canvas store. Switching between World and a Space activates the target manager instead of clearing the scope being left; an authoritative reload of the already-active Canvas still clears that Canvas's stale history. Portal-specific history invalidation and source-to-World Pin routing are gone. Undo/redo filters retired topology before restoring a snapshot while retaining the ordinary live Question-data and transient-field preservation rules.

The keyboard shortcut catalog may retain internal runtime bindings with `hidden: true`; `getKeyboardShortcutSections()` excludes them from the user-facing modal. Removed bindings must be deleted from the catalog rather than left as display-only entries.

Preview Workspace state, rendering, tab/group behavior, Chat isolation, runtime focus requests, persistence, and validation are specified in [preview-workspace.md](./preview-workspace.md); the summary below records only how that subsystem fits into the wider web application.

The Expanded Node Panel derives connected-node navigation from the active Canvas edges without adding persisted navigation state. One relationship-menu trigger sits at the far left before the node title and groups destinations as sources, neighbors, and destinations instead of exposing three persistent toolbar buttons. Node-specific preview actions sit on the right before a divider and the view controls. A `forward` arrow follows the edge's source-to-target endpoints, a `backward` arrow reverses them, and a `both` arrow contributes the neighbor to both source and destination groups; a `none` edge has no directional meaning and appears in the neighbor group. Neighbors follow Canvas node order after missing endpoints, self-loops, and duplicates are removed. Bare Left/Right Arrow actions switch directly when one directional neighbor exists or open the relationship menu focused on the matching group when several exist; neutral connections remain menu-driven so the directional shortcuts do not imply an invented order. Editable controls, search, menus, media controls, and embedded viewers retain arrow-key ownership. Switching calls `openPreviewNode` and does not select or reveal the destination on the Canvas. It opens transiently: navigating between connected nodes is browsing, so it reuses the preview group's inspection slot rather than accumulating a tab per neighbor.

Canvas-wide search keeps its query and results mounted when focus moves elsewhere. Its capture-phase Enter and Arrow navigation owns events from the search input, result list, and non-interactive Canvas targets, including React Flow node wrappers focused by live-follow; editable surfaces and other controls in Chat, Preview, or Canvas retain their native keyboard behavior without requiring the search to close.

The Canvas Layer panel surfaces hydrated `contentMissing` and `artifactMissing` state in two places: each affected row carries a warning status with a kind-specific tooltip, and a count summary below the search and type-filter controls toggles a flat missing-only view. Missing-only filtering intersects with type chips, excludes not-yet-imported external notes, can be cleared from the summary, and exits automatically when the missing count reaches zero. Canvas text search remains the active result surface while its query is non-empty, so the summary preserves its count but disables missing-filter changes until search is cleared.

Preview Workspace is the only right-side presentation surface. `MainLayout` mounts it in the collapsible right column and `CenterArea` hosts only the Canvas. The floating Bot button opens and focuses the most recently active Chat tab, or creates one when none exists; it never collapses the workspace, whose own header control owns that action. Each Canvas persists one workspace containing one or two horizontal groups, semantic node or unbound-Chat targets, active tabs, split ratio, and deterministic activation sequence. Reopening a target activates its existing tab across groups; Open to Side moves it instead of duplicating it. Node opens through `openPreviewNode` are transient by default, including Canvas double-clicks, Layers activation, search results, and connected-node browsing; they reuse one inspection tab per group without demoting existing permanent tabs. Permanent node opens require `{ transient: false }`, as used by canonical Node deep links. The lower-level `openPreviewTarget` retains its permanent default for independent Chat, URL, and creation workflows. Transient tabs use italic titles and an accessible tooltip that identifies the temporary preview and its double-click-to-keep action. Double-clicking the tab or making a persistent mutation through its renderer promotes it in place: Preview Workspace owns the lifecycle transition, while Chat and Expanded Node renderers only report semantic commits such as sending a message, changing an attachment or thread setting, renaming a node, or editing node content. Moving through search results, scrolling, and editing an unsent draft do not promote the tab. Permanent tabs remain open until the user closes them or their target node is deleted; browsing stays bounded by reusing each group's transient inspection slot. The workspace tab strip and embedded Chat or Expanded Node action bars share a 36px height; the tab strip owns the primary title and close action. New conversation creates an independent thread-backed Chat tab in the focused group, and Save Chat as Question converts that tab's target in place. Tabs use shared pointer and keyboard drag sensors, but every drop delegates to the workspace model's `moveTab` action for ordering, cross-group movement, active-tab repair, and empty-group removal. The old single Chat panel, side-by-side centre preview, replace-Canvas mode, feature flag, and Settings toggle no longer exist. See [`docs/proposals/unified-preview-workspace.md`](../proposals/unified-preview-workspace.md).

Explicit node opens create a runtime-only `{ tabId, nonce }` editor-focus request in Preview Workspace. Only the addressed tab receives the request, and its renderer consumes the request after focusing so a later remount cannot replay stale intent; ordinary tab activation does not request editor focus.

Question conversation opens similarly create a runtime-only `{ tabId, position, nonce }` request for `last-user` or `bottom` message positioning. The mounted MessageList consumes it only after history hydration and successful positioning, so inactive tabs and later remounts cannot replay stale scroll intent. Preview Workspace layout remains local UI state and is written synchronously both when switching Canvas and during the consolidated `beforeunload` flush.

The split-group separator is the single visual boundary between groups, with an 8px pointer target overlaid symmetrically around a 1px rule so the hit area does not create a visible gap between tab strips; focused groups do not add a second perimeter ring. Pointer movement is tracked on `window` until pointer up or cancellation, so dragging either direction remains continuous after the pointer leaves the narrow separator.

Node creation keeps spatial placement separate from presentation. Explicit `user-created` Note/Text placement (for example the Canvas toolbar) requests immediate editing, and toolbar Question placement enters compose through its dedicated flow. Content-derived creation (`user-excerpt`, `user-from-chat`, uploads, and imports) creates and selects its Canvas node without opening or replacing a Preview Workspace tab, so repeated extraction remains continuous.

Local HTML Web Nodes carrying `data.interactiveView` mount as Interactive Views through a dedicated renderer route whose CSP permits only self-contained inline/data/blob content and denies network connections, form submission, nested frames, workers, objects, and external navigation. `WebPreview` removes `allow-same-origin`, keeps only script/form sandbox capabilities, transfers one Host-owned `MessagePort` on the expected document load, and closes rather than reconnecting if that document navigates itself; all later iframe intents use the port. `useInteractiveViewBridge` loads persisted state and typed binding snapshots, rejects oversized, replayed, or excessive requests, dispatches only declared actions, performs native Node/thread navigation against Host-derived binding references, and closes the port, focus listener, and polling timer on unmount or mode change. Binding refresh runs only while mounted and visible. The iframe receives no token, cookie, API route, Host DOM, Electron bridge, or arbitrary query capability.

Each Chat renderer receives a required `ChatSession` and owning workspace tab ID from `PreviewRenderer`; there is no globally current Chat thread or Question replay pointer. Messages, drafts, loading, binding, model settings, pending attachments, and mutable compose mode live on `threadsById[threadId]`; `threadMap` stores only each Canvas's canonical unbound Chat seed. History and stream hooks validate that their owning tab still presents the same target before applying delayed work. An authored Question node's agent mode remains authoritative for replay and follow-up turns.

---

## 5. Internationalisation

The web app uses [`i18next`](../../apps/web/src/i18n/index.ts) with
`react-i18next` for React components.

- Initialise i18n once from [`main.tsx`](../../apps/web/src/main.tsx) by
  importing `apps/web/src/i18n`.
- Locale resources live under
  [`apps/web/src/i18n/resources`](../../apps/web/src/i18n/resources). Keep
  user-facing UI copy in translation files rather than inline strings.
- React components should call `useTranslation()` and pass translated strings
  into common components (`Button.title`, `Loading.message`, `Modal.title`,
  etc.).
- Non-React code may import the shared `i18n` instance and call `i18n.t(...)`
  for user-facing copy (for example toast messages emitted from stores).
- Shared package constants remain token/fallback oriented. Web helpers such as
  [`translateColorOptions`](../../apps/web/src/i18n/colors.ts) map stable
  shared tokens to localised labels at the UI boundary.
- Server API errors expose stable `code` values and English `message`
  fallbacks. Client UI should branch on the code and localise the displayed
  copy; show the server message only as a fallback for unknown errors.
- Date/number formatting should use `i18n.language` rather than the implicit
  browser default when the formatted value is part of translated UI.

`i18next-icu` is intentionally not part of the baseline. Use i18next's built-in
plural suffixes (`key_one`, `key_other`) for simple counts; add ICU only if the
product starts relying on complex plural/select/date message patterns.

---

## 6. Node & edge stacking (z-order)

The **Layers panel / `nodes` array order is the persisted stacking authority.** At rest, later in the forest means painted on top. A plain node ordered after a Frame covers that Frame **and its entire subtree**; ordered before, it is covered by the whole subtree. Sole node selection temporarily elevates that node and its descendants for interaction; deselection or multi-selection restores the normal forest mapping without changing array order or stored z values.

React Flow's default `zIndexMode: 'auto'` does **not** honour this: it forces
every child above its parent and lifts framed top-level frames by a fixed band,
so a framed subtree always floats above unframed siblings regardless of order.
To make array order authoritative we render with
[`<ReactFlow zIndexMode="manual">`](../../apps/web/src/components/Panels/Canvas/Canvas.tsx)
and derive every `zIndex` ourselves in the **render layer**:

- [`assignNodeZIndices`](../../packages/shared/src/canvas-engine/container/zorder.ts)
  — a depth-first walk of the parent/child forest (parents before children,
  siblings in array order) assigns each node a contiguous z. Children land
  immediately above their frame; a later sibling out-ranks the whole preceding
  subtree.
- [`selectionZOrder`](../../apps/web/src/components/Panels/Canvas/selectionZOrder.ts) — when exactly one node is selected, offsets its subtree above the maximum base z while retaining relative forest order. Selecting a nested child does not elevate its ancestors or sibling branches; selecting a Frame includes its descendants so its background cannot cover them. Zero or multiple selected nodes reuse the base map unchanged. React Flow keeps `elevateNodesOnSelect={false}`; its implicit selection bump is not used.
- [`edgeZIndex`](../../packages/shared/src/canvas-engine/container/zorder.ts) — an
  edge floats at the z of its highest **framed** endpoint (0 when both endpoints
  are top-level), mirroring React Flow's old auto-mode edge behaviour, which
  manual mode otherwise drops. `Canvas.tsx` writes this onto `edge.zIndex`; the
  label portal reuses it via
  [`getEdgeRenderZ`](../../apps/web/src/components/Panels/Canvas/edges/edgeZ.ts).

These are derived at render only. The engine's legacy `zIndex: -1` writes on
framed children are now harmless vestigial values (the render layer overrides
them), so **persistence, diff/delta, the server executor, and realtime sync are
untouched.** The z-wrapping in `Canvas.tsx` is cached by source-node/edge ref so
selection toggles don't break xyflow's per-element `React.memo`. React Flow spreads `node.style` after its internal root z, so `withoutNodeStyleZIndex` removes any inline `style.zIndex` from a cached, render-only style copy, including during geometry previews. Other style properties, positions and measurements remain untouched. In-node resize and connection controls rise with the actual node root; floating toolbars keep their existing portal. No enlarged hit target or transparent blocking overlay is added. Edges use the same presentation z map so edges inside an elevated Frame remain above its background; edge selection itself still does not elevate edges.

---

## 7. Related docs

Canvas navigation has two operations: `revealNodesOnCanvas` preserves zoom and applies only the minimum pan needed to expose a target, while `fitNodesOnCanvas` adjusts zoom and centres the target bounds in the uncovered area. Search results (including edge endpoints), Layers, chat reference badges, node deep links, and interactive Web view navigation use reveal. Navigation that opens Preview opens it first, then requests reveal. Reveal reads node bounds, the current viewport, and final panel insets on the next animation frame after the layout update; only the latest request per Canvas wrapper runs, and detached or zero-size canvases are skipped. It does not wait for the panel slide animation to finish. The fit control and initial canvas loading without a saved viewport use fit. There is no separate forced-centre navigation operation and application code does not call React Flow's native `fitView`. Reveal uses sine ease-in-out timing with a linear viewport path, keeping zoom constant throughout the pan. Opening Preview alone does not request node reveal.

Canvas zoom is bounded by the shared web configuration constants `MIN_ZOOM = 0.01` and `MAX_ZOOM = 5` (1%–500%). React Flow, custom pinch gestures, and overlay-aware fit operations reuse those limits. Oversized reveal targets use leading-edge alignment; there is no separate size-only centring mode. Both side-panel resize handles share one pointer-scoped lifecycle, read current geometry during dragging, and stop on pointer release, cancellation, window blur, or unmount. Panel width updates are immediate; only panel opening and closing animate via transforms.

Canvas pan and zoom are local UI state rather than canvas topology. [`canvasStore`](../../apps/web/src/store/canvasStore.ts) records the last viewport under a canvas-specific `localStorage` key, allowing both browser and Electron users to reopen a canvas at the previous view without creating server writes or sharing a viewport across devices. The write is debounced so a per-frame animation persists once; reads and canvas switches flush any pending write first.

Canvas fills the work area beneath the Layers and Preview overlays. Both side panels use the same 220ms transform transition; opening, closing, resizing, or splitting them never resizes or pans Canvas. Opening a node-associated Preview also leaves the viewport unchanged, even when the panel obscures the node; there is no deferred pan or camera restoration on close. Users can pan manually or invoke explicit search, reveal, focus, and fit actions. `MainLayout` publishes the final covered widths as `--canvas-inset-left` and `--canvas-inset-right`; explicit positioning operations use the remaining visible rectangle. Explicit reveal uses a 24px safe margin without zooming; oversized targets expose their top-left content. Reduced motion disables panel animation. Actual window resizes retain centre compensation; the viewport has no CSS transform transition.

The bottom creation toolbar is portalled into a pointer-transparent layer above both panels and remains horizontally centred on the uncovered Canvas area, using the shared left and right insets. Opening, closing, or resizing either panel updates only its horizontal position; its dimensions, styling, spacing, and 24px bottom offset remain unchanged. Focusing or pressing inside a side panel slides it down out of view in 160ms and makes its contents inert only when its expanded footprint overlaps that panel; otherwise it remains visible. Leaving the panels, collapsing the owning panel, or resizing until the overlap clears slides it back up. The overlap check uses the measured toolbar width, its uncovered-area centre, and final panel widths, independent of the hiding transform. Moving the pointer alone does not change visibility. Panel menus retain their owner's focus state. Only toolbar controls receive input; the rest of its host is click-through. Other Canvas controls follow the uncovered area's edges. Canvas itself is an isolated stacking context below the panel surfaces. Panels have contained scrolling and own pointer and keyboard interaction. Collapsed contents are inert, while the shell hides only after its exit transition. The layout uses `overflow: clip` so focusing an offscreen descendant cannot scroll the entire work area. Global Canvas keyboard, paste, and tool shortcuts yield to panel, menu, listbox, and dialog targets. Programmatic composer focus continues to use `preventScroll`. Fullscreen Preview unmounts Canvas and its toolbar.

Layers presents the live Canvas hierarchy as a WAI-ARIA tree. One visible row participates in roving tab focus; Up/Down traverse visible rows, Right expands a collapsed Frame or moves to its first direct child, Left collapses an expanded Frame or moves to its parent, Home/End move to the tree bounds, and Enter/Space perform primary activation. Activating a collapsed Frame expands it; activating the same selected, expanded Frame again collapses it, while initially selecting an already-expanded Frame leaves its descendants visible. The disclosure `Button` remains a separate focusable control for explicit state changes. Each reorderable row exposes a separate keyboard-accessible drag handle backed by dnd-kit's pointer and keyboard sensors, preventing reorder keys from conflicting with tree activation. Modifier pointer activation retains range and additive Canvas selection without opening Preview.

The Layers shell stays mounted, but its ordinary tree is not mounted on an initially collapsed Space. Opening mounts the tree immediately; closing retains it through the slide transition, then unmounts it on the overlay's own `transform` transition end, with a 300ms fallback for missing motion events. Reopening cancels pending removal. Type filters remain in their existing owner; the shell retains tree scroll position, roving focus ID, and range-selection anchor across remounts without stealing DOM focus. Scroll and tree navigation state reset on Space changes, and the tree revalidates retained focus against live rows. Active search results stay mounted while the panel is collapsed, preserving their existing Virtuoso scroll state, active result, and Canvas highlights without replaying first-result selection on reopening. The ordinary tree uses delayed mounting, not row virtualization: an open tree still renders all expanded, filtered rows.

The ordinary Layers hierarchy omits Sketch nodes by default to keep freehand input from overwhelming the list. Type chips remain an explicit whitelist: selecting Sketch switches to the flat Sketch-only result view, and an empty whitelist still sends no type restriction to Canvas-wide search, so search remains comprehensive. The explicit missing-node view also includes missing Sketch nodes so its count and result set remain consistent.

Primary activation validates the row against live Canvas nodes, expands collapsed ancestors, replaces Canvas selection, and reveals the target only while both the React Flow instance and Canvas wrapper are mounted. Preview-capable nodes then open or activate their semantic Preview target with `transient: true` in every layout, including Preview fullscreen where Canvas is unmounted; existing permanent tabs remain permanent. A node already active in a visible Preview group is not reopened, preventing repeated Note editor-focus requests. Frame and legacy Group rows toggle their own disclosure on repeated primary activation and reveal direct children but never invoke Preview; activating an empty Frame emits an accessible status toast. Existing-thread Questions reuse the passive Preview opening contract without creating a thread, focusing the composer, sending input, or realizing Agent work; idle Questions and unsupported node types remain Canvas-only.

- [canvas-command-architecture.md](./canvas-command-architecture.md) — the command/engine model (shared, server + web).
- [agent-context.md](./agent-context.md) — how the web assembles agent context.
- [api-design.md](./api-design.md) — HTTP/SSE contract rules the `api/` clients follow.

Viewport geometry lives in [`focusNodesOnCanvas.ts`](../../apps/web/src/components/Panels/CanvasLayerPanel/focusNodesOnCanvas.ts); [`Canvas.tsx`](../../apps/web/src/components/Panels/Canvas/Canvas.tsx) handles window resizes. [`MainLayout.tsx`](../../apps/web/src/pages/CanvasPage/MainLayout.tsx) owns overlay bounds and the toolbar host. [`overlay-panels.spec.ts`](../../apps/web/e2e/overlay-panels.spec.ts) exercises the real layout through the isolated browser harness.

---

## 8. External user handbook

The web application does not contain handbook pages, assets, or a `/docs/*` route. Product actions call the leaf-level [`openUserHandbook()` helper](../../apps/web/src/config/handbook.ts), which validates an absolute URL and opens it in a separate browser context.

Production requires `VITE_HANDBOOK_URL`; the checked-in [`apps/web/.env.production`](../../apps/web/.env.production) supplies the Microsoft Huabu Pages URL, and deployment environments may override it. `pnpm dev` and `pnpm dev:desktop` use that public handbook by default and honor a `VITE_HANDBOOK_URL` override, such as a separately running Huabu repository handbook on localhost. Other development entry points resolve `/docs/` against the current page origin when the variable is unset. Production accepts HTTPS only, while development also accepts HTTP on loopback hosts. Electron continues to deny renderer child windows and sends HTTP(S) targets to the operating system through its existing `setWindowOpenHandler` in [`apps/desktop/src/main.ts`](../../apps/desktop/src/main.ts).

The handbook is owned, built, and deployed from the public [microsoft/Huabu repository](https://github.com/microsoft/Huabu/tree/main/apps/docs); Huabu does not carry a second user-handbook application.

`pnpm start:web` serves the compiled SPA and API from one production-style Fastify process. Before importing the Server bundle, its launcher selects the first available port at or above `SERVER_PORT`/`PORT` (default 3001) and writes the resolved value to `SERVER_PORT`; the shared port probe also protects `dev` and `dev:desktop` from loopback-versus-wildcard binding conflicts.

Network deployment follows the single-owner boundary in [`deployment-security.md`](./deployment-security.md). Non-loopback `start:web` binds fail closed unless allowed hosts and complete Basic Auth are configured. Vite keeps zero-configuration loopback development but rejects non-loopback clients before serving assets or proxying APIs unless they pass the same Basic Auth gate. Settings reads the redacted deployment readiness endpoint and disables credential mutations when the standalone secret store is read-only.

Settings → General also owns the server-persisted **Automatically accept Agent Space changes** preference. [`GeneralSettings.tsx`](../../apps/web/src/components/Settings/sections/GeneralSettings.tsx) loads and updates it through the owner-only Agent Change Review API, optimistically reflects a toggle, and restores the last confirmed value on write failure. The preference is application-global: it suppresses future pending Keep/Revert records but does not delete existing records or convert current-session Canvas undo into durable Revert.

## 9. Desktop troubleshooting actions

The packaged desktop app exposes three support actions without granting the renderer general filesystem or Electron access: reveal the canonical Server log, open Chromium Developer Tools, and copy non-sensitive system information (Huabu version, OS release, CPU architecture, and Electron version). The sandboxed preload bridge exposes only these fixed operations under `electronBridge.diagnostics`; filesystem paths and shell calls remain in the main process. Packaged builds resolve the log below Electron's `userData/data`; `dev:desktop` passes the source Server's `apps/server/data` location to both processes through `HUABU_DATA_DIR`, so the same action always reveals the log written by the active Server.

The Electron-owned `userData` tree (Chromium storage and Electron's own logs / crash dumps) is partitioned by `app.setName`, keyed off whether `EXTERNAL_SERVER_URL` is set (the signal that `scripts/dev-desktop.mjs`'s HMR orchestrator is driving this run): `dev:desktop` anchors on `Huabu Dev` to isolate its actively-changing tsx-watch/Vite code from real user state, while both a packaged install and `pnpm start:desktop` (an unpackaged run of the exact production bundle, used as a pre-release smoke test) anchor on `Huabu` and intentionally share the same on-disk state. The Server data dir follows separately — `start:desktop` derives it from `<userData>/data` (inheriting `Huabu`), while `dev:desktop` overrides it to the in-repo `apps/server/data`. Workspace membership, restore state, and MRU ordering live in the Server-owned `<dataDir>/storage/disk/workspaces.json`. The former port-agnostic `<userData>/workspace.json` is deprecated and read only as a one-time upgrade source when the new registry does not yet exist. Credentials are not affected by this name split: only `start:desktop` / packaged installs use the `safeStorage`-backed `<userData>/data/secure-secrets.json` (and both resolve to `Huabu`, so they share it), whereas `dev:desktop` skips `safeStorage` entirely and persists secrets to `apps/server/data/encrypted-secrets.json` via `HUABU_SECRET_KEY`.

The native macOS Help menu and the Windows/Linux in-app application menu reuse the fixed operations exported by [`useElectron.ts`](../../apps/web/src/hooks/useElectron.ts) and add localized feedback at the UI boundary. On Windows and Linux, Troubleshooting is a side-opening submenu composed from the shared `DropdownMenu` primitives rather than a flat group of support actions. The browser build omits the actions because the diagnostics bridge is absent.

## Code entry points

| File/dir                                                                                                                               | Responsibility                                                                                                            |
| -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| [`apps/web/src/App.tsx`](../../apps/web/src/App.tsx)                                                                                   | Product router; deliberately has no handbook route.                                                                       |
| [`apps/web/src/config/semanticZoom.ts`](../../apps/web/src/config/semanticZoom.ts)                                                     | Node far-zoom opt-in, default 20%/24% and Note-only 25%/28% hysteresis, reading thresholds, and text-fit policy.          |
| [`apps/web/src/hooks/useNodePresentation.ts`](../../apps/web/src/hooks/useNodePresentation.ts)                                         | Overview-initialized presentation history, viewport visibility, and interaction eligibility; Office has no reading layer. |
| [`apps/web/src/components/Nodes/SemanticPlaceholder.tsx`](../../apps/web/src/components/Nodes/SemanticPlaceholder.tsx)                 | Text-only minimal layer inside the existing node shell.                                                                   |
| [`apps/web/src/components/Nodes/semanticZoom/FarZoomLabel.tsx`](../../apps/web/src/components/Nodes/semanticZoom/FarZoomLabel.tsx)     | Production/playground title and description renderer with complete-line measurement.                                      |
| [`apps/web/src/components/Nodes/semanticZoom/FarZoomText.tsx`](../../apps/web/src/components/Nodes/semanticZoom/FarZoomText.tsx)       | Shared normal wrapping, emergency long-word breaks, and punctuation boundaries.                                           |
| [`apps/web/src/components/Nodes/design/farZoomDesign.ts`](../../apps/web/src/components/Nodes/design/farZoomDesign.ts)                 | Shared fixed-screen typography, insets, gap, and description line budget.                                                 |
| [`apps/web/src/pages/playground/ZoomReadabilityStudy.tsx`](../../apps/web/src/pages/playground/ZoomReadabilityStudy.tsx)               | Shared node-rendering and production Frame-region comparison.                                                             |
| [`apps/web/src/pages/playground/farZoomPresentation.ts`](../../apps/web/src/pages/playground/farZoomPresentation.ts)                   | Production node/Frame-region adapter and local Frame header experiment.                                                   |
| [`apps/web/src/config/handbook.ts`](../../apps/web/src/config/handbook.ts)                                                             | Validate and open the canonical external handbook URL.                                                                    |
| [`apps/web/src/hooks/useElectron.ts`](../../apps/web/src/hooks/useElectron.ts)                                                         | Typed Electron bridge access, fixed support operations, and copied system-information format.                             |
| [`apps/web/src/store/conversationOwner.ts`](../../apps/web/src/store/conversationOwner.ts)                                             | Generic presentation-anchor/conversation-owner routing and owner-aware lifecycle writes.                                  |
| [`apps/web/src/store/previewWorkspace/`](../../apps/web/src/store/previewWorkspace)                                                    | Per-Canvas tab/group topology, persistence, target actions, MRU protection, and selectors.                                |
| [`apps/web/src/components/Panels/PreviewWorkspace/`](../../apps/web/src/components/Panels/PreviewWorkspace)                            | Tab strips, groups, split interaction, drag/drop, and target-to-renderer dispatch.                                        |
| [`apps/web/src/hooks/useChatSession.ts`](../../apps/web/src/hooks/useChatSession.ts)                                                   | Required renderer-local Chat thread and conversation-owner address.                                                       |
| [`apps/web/src/components/Common/Toast.tsx`](../../apps/web/src/components/Common/Toast.tsx)                                           | Global toast state, tone-specific presentation, duration defaults, and dismissal.                                         |
| [`apps/web/src/components/Panels/ExpandedNodePanel/`](../../apps/web/src/components/Panels/ExpandedNodePanel)                          | Expanded content rendering and edge-connected node navigation.                                                            |
| [`apps/web/src/components/Nodes/web/useInteractiveViewBridge.ts`](../../apps/web/src/components/Nodes/web/useInteractiveViewBridge.ts) | Sandboxed Interactive View MessagePort, action grants, binding refresh, and native navigation.                            |
| [`apps/desktop/src/preload.ts`](../../apps/desktop/src/preload.ts)                                                                     | Narrow sandbox bridge for native menu and diagnostics operations.                                                         |
| [`apps/desktop/src/main.ts`](../../apps/desktop/src/main.ts)                                                                           | Electron window security, external URLs, and fixed diagnostics IPC handlers.                                              |
| [`scripts/start-web.mjs`](../../scripts/start-web.mjs)                                                                                 | Production-style web launcher and dynamic Server port selection.                                                          |
| [`scripts/dev-ports.mjs`](../../scripts/dev-ports.mjs)                                                                                 | Shared loopback/wildcard-aware development port selection.                                                                |
| [`apps/web/src/components/Panels/CanvasLayerPanel/`](../../apps/web/src/components/Panels/CanvasLayerPanel)                            | Layers hierarchy, accessible tree activation, disclosure, drag/reorder, and Canvas reveal.                                |
