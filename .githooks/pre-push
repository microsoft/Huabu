#!/usr/bin/env sh
# Optional local hook for fast pre-push feedback.
# Enable once per clone with:  pnpm run hooks:install
# Disable with:                pnpm run hooks:uninstall
#
# This is NOT a substitute for CI. CI (.github/workflows/ci.yml) is the
# authoritative gate; these hooks only give you quicker local feedback.
#
# Skip a single push with:  git push --no-verify

pnpm run typecheck
