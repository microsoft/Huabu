# Complex document

Nested lists three levels deep:

- Level 1 a
  - Level 2 a
    - Level 3 a
    - Level 3 b
  - Level 2 b
- Level 1 b

Task list:

- [x] Done
- [ ] Pending
- [ ] Another pending

GFM table with alignment and an empty cell:

| Left | Center | Right |
| :--- | :----: | ----: |
| a    |   b    |     1 |
|      |        |     2 |
| c    |   d    |     3 |

Fenced code block with a language tag:

```ts
export function add(a: number, b: number): number {
  return a + b;
}
```

A paragraph that contains a hard line break.\
The line after the break stays in the same paragraph.
