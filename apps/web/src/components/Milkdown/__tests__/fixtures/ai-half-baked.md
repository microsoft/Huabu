# Half-baked AI output

A paragraph mixing 中文 and English with a stray dollar in price text: 价格 $5。

Unclosed fence below — Milkdown should recover by treating the rest as code:

```ts
const partial = () => {
  console.log('still typing...');
```

Dangling list with mixed markers:

- one
- two

* three (different marker on purpose)

Inline `code with $math-like text$` should not be treated as a formula.
