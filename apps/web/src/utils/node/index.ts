export {
  getNodeSize as getMeasuredNodeSize,
  getLayoutNodeSize,
  NODE_TYPE_TO_PREFIX,
  extractLabelNumber,
  generateNextLabel,
  deduplicateLabel,
  getNodeDefaultSize,
} from '@sediment/shared/canvas-engine';
