# Portal/Pin retirement browser acceptance — 2026-09-15

## Latest status — full4 accepted after undo ordering and stale-preprocess fences

**4 passed / 0 failed / 0 skipped / 0 flaky / 0 retries.** One unchanged full four-case run started **2026-09-15 03:55:01.058 UTC** and completed in **88.676 seconds**, using one Chromium touch-project worker. No timing failure occurred, so no second run was needed. This is the latest acceptance status; every older result below is historical, including former blockers and claims that the World cases were not rerun. Standard report/artifact directories now contain this successful full4 run.

- **World membership, Open Space, live Note refresh, test-Space deletion:** passed all functional assertions and strict audit, **40.639 s**.
- **World preview move, resize, reload, local viewport isolation:** passed all functional assertions and strict audit, **18.907 s**.
- **Ordinary Note/Frame create, move, native copy/paste, delete/undo, reload:** passed clipboard, restored content, geometry and strict audit, **23.922 s**.
- **Actual worktree and disposable-storage isolation:** passed listener cwd, registry and World identity assertions, **1.808 s**.

### Restored-body and trace evidence

- The ordinary test retained its origin-scoped clipboard grant and completed-copy wait; the application produced the actual native HTML payload before paste. Both Note bodies were verified before deletion, then all four IDs and their geometry were verified after undo/reload. The full-success annotation was reached.
- Both reloaded content attachments contain exactly `Ordinary note acceptance\n\nCopy and undo marker`, with revision `icliex` and no `contentMissing` field: original Note `node-e0dd7e28-b084-4fff-b202-680fa842c652` and restored copied Note `node-7003891c-439c-4c45-953f-77ab5e624de6`. The roundtrip screenshot was visually inspected: both bodies are readable and there are no missing-file banners.
- The ordinary trace shows successful copied Note/Frame DELETEs at **03:56:18.694 UTC**, followed by the two-node structure save (version 5 → 6) at **03:56:19.672**. After undo, the four-node structure PUT (version 6 → 7) starts at **03:56:23.458** and returns HTTP 200. Its trace monotonic start plus duration is **82597.592 + 6.649 = 82604.241 ms**; the restored Note and Frame content PUTs start later, at **82604.566 / 82604.611 ms** (**03:56:23.465 UTC**). Thus the structure request completes before the restored body writes begin, not merely before they finish. The Note PUT includes the full marker body with `expectRev: "3horh"` and returns nonempty revision `icliex`; Frame returns `1crc3bu`.
- All four browser-audit attachments have empty `errors`, `failedResponses`, and `forbidden` arrays: **315 browser API requests** (151 lifecycle, 95 geometry, 66 ordinary, 3 isolation), **zero HTTP 400 or other failed HTTP responses, zero console/page errors, zero legacy references/Pin command requests, and zero forbidden external/Agent requests**.
- All four network traces were additionally inspected for preprocessing application-level failures, including HTTP-200 bodies: **6 actual preprocessing POSTs (4 Note, 2 Frame), all `success: true`; zero `spacePreview`/`sketch` POSTs; zero missing-sidecar errors**. No restored-node preprocessing POST occurred after undo in this run. This verifies the current four workflows, not every possible stale-callback interleaving or real Agent behavior.

### Validation, isolation, cleanup, and retained evidence

- Read the current spec, focused/base configurations, teardown and acceptance report before running. Reused the existing isolated harness unchanged: exactly two sanitized services, backend **3197**, frontend **5297**, explicit loopback proxy, disposable Disk workspace/data/HOME, and `reuseExistingServer: false`. Execution: `HUABU_STRUCTURED_BACKEND=disk E2E_BROWSER_CHANNEL= pnpm --dir apps/web exec playwright test --config playwright.portal-retirement.config.ts` from the Huabu root. No 5294 service/page, other worktree, real workspace or credential store was used or altered.
- Strict standalone E2E TypeScript passed with `--ignoreConfig --noEmit --skipLibCheck --esModuleInterop --strict --target es2022 --module esnext --moduleResolution bundler --types node`, scoped to the spec and focused config. Scoped Prettier includes those two files and this report. No root scripts, production fixes, test/config edits, test weakening, commits, or production-suite validation were performed in this verification task.
- Actual listener PIDs **39160** (backend) and **39168** (frontend) had cwd in this Huabu worktree's server/web directories. Workspace/data/HOME paths ended `39125-82846a11-9225-4bb1-b5ab-cb28a1efbf48`; exact paths and registry identity are in `isolation-proof`. Teardown was verified: both paths absent, both PIDs absent, no processes referencing either path, and no listeners on 3197/5297. No manual process termination or storage deletion was needed. Evidence artifacts are intentionally retained, not live test storage.
- [HTML report](../playwright-report/portal-retirement/index.html), [JSON results](../playwright-report/portal-retirement/results.json), [all case artifacts](../test-results/portal-retirement), and [ordinary roundtrip screenshot](../test-results/portal-retirement/portal-retirement-ordinary-b8191-py-paste-delete-undo-reload-touch/ordinary-frame-note-roundtrip.png) contain this run. Previous focused-run evidence was archived at `/var/folders/qz/cqzsh0xs0z1g92t3ty9t2f5w0000gn/T/huabu-portal-before-final-full4-mv3_4b_j` before execution.
- Logs: `/tmp/huabu-portal-final-full4-run1.log` (execution), `/tmp/huabu-portal-final-full4-evidence.log` (results/attachments), `/tmp/huabu-portal-final-full4-trace.log` (decoded mutation order and preprocessing), `/tmp/huabu-portal-final-full4-cleanup.log` (cleanup), `/tmp/huabu-portal-final-full4-typecheck.log` (strict TypeScript), `/tmp/huabu-portal-final-full4-prettier-before.log` (initial scoped formatting), and `/tmp/huabu-portal-final-full4-validation.log` (final validation). The source fingerprint baseline is `/tmp/huabu-portal-final-full4-source-before.log`; only this acceptance report is intentionally updated by this task, preserving all pre-existing production/test changes.

## Historical — authorized undo persistence fix, focused result

The production ordering fix passed the unchanged **ordinary Note and Frame create, move, copy/paste, delete/undo, reload** case on 2026-09-15: **1 passed, 0 failed, no retries**, 24.6 seconds for the case (27.5 seconds total). The test used the existing isolated Disk configuration on 3197/5297 with disposable HOME/data/workspace; no 5294 service or real user data was used. Both reloaded Note bodies and all existing strict browser/HTTP/Agent audit assertions passed. The spec and configuration were not edited in this fix. Standard report directories now contain this focused successful run; the older sections below remain historical evidence, not current failure status. The World cases were not rerun in this focused run. The isolated services had no listeners after teardown.

The fix reuses the content queue and structure-save path with completion-tracked deletes and generation-scoped restore holds; it does not change debounce delays, backend tombstones, endpoints, or content CAS. Thirteen deterministic store/queue regressions cover pending deletes, rapid redo, obsolete acknowledgements, save failure/Retry, version conflicts, Canvas switching, missing snapshots, and ordinary content saves. Web TypeScript and the expanded focused suite pass: 124 tests in 18 files. The expanded suite printed a Happy DOM teardown fetch-abort diagnostic but reported no failed tests; the new restore suite stubs unrelated event transport and issues no real requests. No commits or root formatter/linter were run.

## Historical clipboard-only follow-up

Status: **Normal native clipboard path verified; full ordinary workflow is NOT accepted because undo/reload loses the restored copied Note body.** This latest follow-up changed only the spec, isolated configuration, and this report. No production code, base harness, fallback implementation, root format/lint, or commits were changed or run.

## Latest test-only clipboard permission correction

- The ordinary test alone grants `clipboard-read` and `clipboard-write` through `context.grantPermissions`, scoped to `http://127.0.0.1:5297`. A fresh automation context does not inherit a normal user's browser permission state. Earlier denied-permission fallback/race evidence describes that denied path, not normal product usage and not evidence that a production clipboard fix is needed.
- After real UI creation, movement and two-node marquee selection, the page is brought to the foreground and document focus is asserted. Only stale clipboard contents are cleared; no copied payload is injected and no application store is read or mutated to bypass the UI. The existing initial plain-text Note fixture remains unchanged.
- Native `ControlOrMeta+c` must complete before native `ControlOrMeta+v`: `expect.poll` waits for the exact readable Note text plus Frame label from `navigator.clipboard.readText()`. The current clipboard contract writes readable text and node-bearing HTML in one atomic `ClipboardItem`; the test reads actual `text/html`, decodes its `data-huabu-nodes` attribute, and reuses `parseHuabuClipboard` to verify the current canvas ID, the exact selected Note and Frame IDs/types, exactly two nodes, and the Note body marker. Missing HTML fails rather than accepting the denied fallback's plain-text JSON.
- After native paste, the unchanged assertions verify two Notes, two Frames, four persisted nodes, two selected copied nodes, and both persisted Note bodies before deletion. The `native-selection-clipboard` attachment and `clipboard-checks` annotation preserve this completed checkpoint independently of the later undo failure. Strict console/page/HTTP and forbidden external/Agent audits remain unchanged; no permission errors were suppressed.
- The focused configuration now uses `defineConfig({ ...base, ...overrides })` composition, with explicit `use` inheritance. Multi-argument `defineConfig(base, overrides)` concatenates `webServer` arrays and previously retained the base services as well as the isolated pair. Direct configuration evaluation now confirms exactly two services: sanitized backend 3197 and frontend 5297, with the latter proxying only to 3197 and both refusing server reuse. The historical claims below that the older configuration launched only this pair were not established by checking these two ports alone and must not be relied on.

## Latest execution results

Two full four-case runs were executed with one worker and zero automatic retries. Run 1 started **2026-09-15 02:54:53.340 UTC**, lasted **90.824 s**, and returned **3 passed / 1 failed / 0 skipped / 0 flaky**. Native copy succeeded without clipboard warnings, but the new test initially parsed readable `text/plain` as legacy JSON and timed out before paste. Trace readbacks showed the exact readable Note/Frame text, not a permission denial. The assertion was corrected to the current HTML payload contract; this first run is not clipboard roundtrip acceptance.

Run 2 started **2026-09-15 02:57:29.030 UTC**, lasted **88.510 s**, and returned **3 passed / 1 failed / 0 skipped / 0 flaky**:

- **World membership, Open Space, live Note refresh, test-Space deletion: passed**, including the strict audit.
- **World preview move, resize, reload, local viewport isolation: passed**, including the strict audit.
- **Ordinary Note/Frame workflow: clipboard checkpoint passed; full case failed after delete, undo and reload.** Both Note bodies were persisted before deletion; deletion left only original IDs, undo restored all four IDs, and reload displayed two Notes and two Frames. The restored copied Note `node-9a597f14-eccf-4aa2-8423-d762c5cf4940` then returned `content: ""` and `contentMissing: true` with HTTP 200, failing `Reloaded Note ... retains its body`. The original Note retained its marker. Later per-node checks and the full-success annotation were not reached. The known unrelated undo/storage failure was left intact; no further test run, assertion weakening, sidecar recreation, or production fix was attempted.
- **Worktree and disposable-storage isolation: passed**, including actual listener cwd, registry path, and World identity.

Run 2 recorded **317 browser API requests**: 151 lifecycle, 95 geometry, 68 ordinary, and 3 isolation. All four audit attachments have empty `errors`, `forbidden`, and `failedResponses` arrays. There were zero HTTP failures, console/page errors, legacy endpoint/Pin command requests, and forbidden external/Agent requests. The ordinary trace contains no clipboard warning. The HTTP-200 content loss is detected by the unchanged content assertion, not hidden by the successful browser audit. Normal clipboard behavior is proven in this isolated run; ordinary end-to-end acceptance is not.

## Latest validation, evidence, and cleanup

- Standalone strict TypeScript for the spec/config passes with `--ignoreConfig --noEmit --skipLibCheck --esModuleInterop --strict --target es2022 --module esnext --moduleResolution bundler --types node`. Scoped ESLint exits 0 with **0 errors and 23 pre-existing non-null-assertion warnings**; an introduced import-order error was corrected before either browser run. Scoped code Prettier, editor diagnostics, and `git diff --check` pass. No production or repository-wide typecheck is claimed.
- [HTML report](../playwright-report/portal-retirement/index.html), [JSON results](../playwright-report/portal-retirement/results.json), and [per-case artifacts](../test-results/portal-retirement) now contain run 2, including the genuine clipboard payload, both reloaded Note responses, browser audits, and isolation proof. The [ordinary roundtrip screenshot](../test-results/portal-retirement/portal-retirement-ordinary-b8191-py-paste-delete-undo-reload-touch/ordinary-frame-note-roundtrip.png) was visually inspected: the original Note remains readable and the copied pair shows missing-file warnings.
- Execution logs: `/tmp/huabu-portal-clipboard-e2e.log` and `/tmp/huabu-portal-clipboard-e2e-run2.log`. Validation logs: `/tmp/huabu-portal-clipboard-typecheck.log`, `/tmp/huabu-portal-clipboard-lint.log`, and `/tmp/huabu-portal-clipboard-prettier.log`. Prior standard evidence was preserved under `/tmp/huabu-portal-before-clipboard-Qa7vut`; run 1 was archived under `/tmp/huabu-portal-clipboard-run1-wo300K`. These evidence archives are intentionally retained, not live service storage.
- Both runs used only this Huabu worktree with disposable HOME/data/workspace. Run 1 suffix: `99932-2fd31e66-5b5e-4b42-8e15-d0d292e98909`, service PIDs **99965 / 99975**. Run 2 suffix: `4419-e6aa6b62-7cdd-42de-a464-1c87b1b0f7fe`, service PIDs **4453 / 4461**. The isolation attachments record exact temporary paths and actual cwd values. Both runs' workspace/data directories and service PIDs were verified absent after teardown; ports 3197/5297 have no listeners. No real 5294 page/service or other worktree was interacted with, stopped, or reconfigured.
- Only [portal-retirement.spec.ts](portal-retirement.spec.ts), [playwright.portal-retirement.config.ts](../playwright.portal-retirement.config.ts), and this English report were edited in this follow-up. All three were already untracked on entry; pre-existing production changes were preserved. The spec received the initial patch and two bounded corrections (import order and current clipboard representation); no further correction was made after the known undo failure.

## Historical record — 2026-09-14 preprocessing follow-up

**Everything below is retained historical evidence, not the latest status.** In particular, its earlier clipboard count failures do not describe the now-verified normal clipboard path, its standard artifact links now point to the latest run, and its older server-isolation claim is qualified above. The preview preprocessing fix remains separate from this test-only follow-up.

Status: **Preview preprocessing 400 resolved; full acceptance remains blocked**. The authorized narrow production fix was tested in two unchanged full four-case isolated runs. Both returned **3 passed, 1 failed, 0 skipped, 0 flaky, 0 automatic retries**. Both World workflows and isolation now pass, including the strict browser audit. The ordinary workflow failed earlier at the post-paste Note count in both new runs; its previously demonstrated undo/reload sidecar content loss remains unfixed and was not reached again. No content assertion, audit, spec, or harness configuration was changed in this production follow-up.

## Current production fix and regression coverage

- [preprocessQueue.ts](../src/store/canvasStore/save/preprocessQueue.ts) reuses the existing queue boundary with one local `isExcludedFromPreprocessing` predicate for `sketch` and `spacePreview`. Scheduling exits before ingestion state or timers are created; delayed and direct unload keepalive sends recheck the latest node type. A delayed eligible-to-excluded change clears the earlier pending state. Missing-content guards remain intact.
- All located production helper callers are inside this queue. `settleNodePreprocess` also calls `queue.schedule`, so no additional helper guard or duplicated server node-type list is needed. No backend schema, API helper, runtime zod import, no-op endpoint request, or history/storage logic changed.
- [preprocessQueue.test.ts](../src/store/canvasStore/save/__tests__/preprocessQueue.test.ts) adds **13 regressions**: two excluded types produce no timers, helper/API requests, pending/error writes, or patches; both latest-type changes are guarded before delayed and keepalive sends; nine ordinary types (`note`, `text`, `question`, `web`, `pdf`, `office`, `image`, `video`, `frame`) retain both paths.
- The targeted save-queue and preprocessing-helper suite passes **33 tests in 5 files**, including the 18-test queue file, on both executions. Final web `typecheck`, standalone strict harness TypeScript, scoped Prettier, editor diagnostics, and `git diff --check` pass. Changed production/test code passes ESLint with `--max-warnings 0`; including the unchanged E2E spec yields **0 errors and 23 existing non-null-assertion warnings**. No root format/lint or commits were run.
- Scoped Prettier passes for the queue, tests, both architecture docs, and unchanged E2E spec/config. This report's new section is formatter-clean; its retained historical results table has pre-existing alignment differences and was not reformatted.
- Architecture updates: [node preprocessing](../../../docs/architecture/node-preprocessing.md) and [Space Preview](../../../docs/architecture/space-preview.md). These four files plus this report are the only files edited by this follow-up. Pre-existing uncommitted Pin retirement changes were preserved.

## Current isolated E2E results

- **World membership, Open Space, live Note refresh, test-Space deletion: passed** all existing functional assertions and strict audit in both runs.
- **World preview move, resize, reload, local viewport isolation: passed** persisted geometry, local viewport, unchanged source/host snapshots, retired-control checks, and strict audit in both runs.
- **Ordinary Note/Frame create, move, copy/paste, delete/undo, reload: failed** at the unchanged post-paste assertion: expected two Notes, received one. Creation, movement and two-node marquee selection completed, followed by native copy/paste key presses. Duplication was not confirmed, so deletion, undo, reload and final body assertions were **not reached in these runs**. No new cause was established or fixed.
- **Worktree and disposable-storage isolation: passed** actual service cwd, workspace registry and World identity checks in both runs.

Run 1 started **2026-09-14 10:01:58.099 UTC**, duration **91.873 s**; run 2 started **10:03:53.280 UTC**, duration **92.080 s**. The second run was a manual unchanged full-suite rerun, not an automatic retry or a test repair. Both runs recorded **282 browser API requests** each: 151 lifecycle, 95 geometry, 33 ordinary, 3 isolation. All four audit attachments in each run have empty `errors`, `forbidden`, and `failedResponses` arrays: **zero HTTP 400, zero other failed HTTP responses, zero console/page errors, zero legacy endpoint/Pin command requests, zero forbidden external/Agent requests**. The final traces contain four actual preprocessing POSTs (three `note`, one `frame`), **zero `spacePreview` or `sketch` POSTs**, and no captured application-level preprocessing failure. This does not establish that the unexecuted undo path succeeds.

The previously reproduced copied-Note `contentMissing: true` loss after undo/reload is still an open separate defect. Its earlier evidence is retained in the historical record below; it is neither fixed nor masked by this queue change. Current results must not be reported as a fresh reproduction of that content assertion or as ordinary workflow acceptance.

## Current evidence and cleanup

- [HTML report](../playwright-report/portal-retirement/index.html), [JSON results](../playwright-report/portal-retirement/results.json), and per-case traces under [test-results/portal-retirement](../test-results/portal-retirement) contain run 2. The live-source screenshot was visually checked and shows the refreshed excerpt; the ordinary failure screenshot shows only the selected original Note/Frame pair, not a completed undo roundtrip.
- Run 1 report/artifacts were copied to `/tmp/huabu-portal-preprocess-fix-report-run1` and `/tmp/huabu-portal-preprocess-fix-artifacts-run1` before run 2. Logs: `/tmp/huabu-portal-preprocess-fix-e2e.log` and `/tmp/huabu-portal-preprocess-fix-e2e-run2.log`.
- Both runs used only `/Users/christy/Documents/code/Huabu`, backend **3197** and frontend **5297**, fresh temporary HOME/data/workspace, sanitized child environment, and `reuseExistingServer: false`. No Agent calls, real data edits, shared 5294 browser, or other worktree were used.
- Run 1 service PIDs **33116 / 33129** and run 2 PIDs **35227 / 35235** were verified absent after teardown. Both ports had no listeners. No process command referenced either isolated run suffix after cleanup.
- Run 1 workspace/data paths ended `33021-de8e0c2b-380a-4c18-9cdf-d22434c361f7`; run 2 ended `35131-f668f26c-7f1c-4f05-9ba1-1ab9090bb6ca`. All four directories under `/var/folders/qz/cqzsh0xs0z1g92t3ty9t2f5w0000gn/T/` were verified absent. Exact paths and actual process directories are recorded in each isolation attachment.

Both runs used `pnpm --dir apps/web exec playwright test --config playwright.portal-retirement.config.ts` from the Huabu root. The strict spec and configuration were left byte-for-byte untouched by this follow-up.

## Historical record — prior test-only follow-up

**Everything below describes the earlier pre-fix runs, not current status.** Its former 400 blocker is resolved by the production fix above; its content-loss evidence remains relevant. Standard report directories now contain the new run rather than the earlier attachments referenced below.

Historical status: **Not accepted: script blockers fixed; runtime blockers remain**. The prior authorized test-only follow-up completed all four Chromium cases with **1 passed, 3 failed, 0 skipped, 0 flaky, and 0 automatic retries** in 98.45 seconds (started 2026-09-14 09:22:33 UTC). Both World workflows completed their functional assertions before failing the unchanged strict network/console audit. The ordinary workflow executed every requested UI operation through reload, but its final restored Note body assertion failed. No production changes or commits were made by that test-only follow-up.

## Scope and harness

The run used the current uncommitted source in `/Users/christy/Documents/code/Huabu`, not clean-main or conversation-titles. [The focused configuration](../playwright.portal-retirement.config.ts) reuses [the existing Playwright harness](../playwright.config.ts), its fresh temporary directories, already-installed Chromium project, and [global teardown](global-teardown.ts). [The spec](portal-retirement.spec.ts) reuses `openNewCanvas`, `paneCenter`, and `readViewportTransform`, the existing Note suite's browser ClipboardEvent pattern, and [the mouse suite's two-node marquee pattern](canvas-mouse.spec.ts). The source Note was edited in a second actual browser tab, not through a mocked scene or injected store state. No browser installation was needed in this follow-up.

No Agent was mocked or invoked. The real embedded transport daemon starts as part of the actual backend, but no Agent turn was submitted. Browser remote requests and Agent execution requests are blocked and recorded; all four final audits recorded zero forbidden requests. Ambient credential/deployment/proxy variables and local dotenv keys are blanked for child services; HOME, data and workspace are disposable. No credentials were configured and no external paid services were used. Preprocessing itself remains real and unmocked, which exposed the failure below. This is not acceptance of real Agent behavior.

## Final test results

| Test                                                                     | Functional coverage actually executed                                                                                                                                                                                                                                                                                                                                                                                                                                      | Final result                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| World membership, Open Space, live Note refresh, and test-Space deletion | Created two Spaces through the UI; compared World targets against the real Space catalogue; verified exactly one preview per target; verified no source editor/node tree inside the projection; checked absence of retired controls and World Add Space Preview; clicked Open Space; edited the source Note in a second tab; observed the mounted World preview refresh; deleted only the test Space through UI confirmation; verified removal from World and after reload | **Failed audit**: all listed functional assertions completed, but 3 preprocess requests returned 400, producing 3 additional console errors                                                                                                                                                                                                                       |
| World preview move, resize, reload, and local viewport isolation         | Mouse-dragged and resized a World preview; checked persisted position/size through the backend and rendered CSS after reload; keyboard-panned/zoomed the local preview; waited for its localStorage debounce; reloaded and checked the SVG viewBox; verified source and host snapshots unchanged by local navigation                                                                                                                                                       | **Failed audit**: all listed functional assertions completed, but 4 preprocess requests returned 400, producing 4 additional console errors                                                                                                                                                                                                                       |
| Ordinary Note and Frame create, move, copy/paste, delete/undo, reload    | Created and moved a Note and Frame through the UI; marquee-selected both; used native keyboard copy/paste; verified two Notes/two Frames and both Note bodies persisted before deletion; deleted only the pasted pair, confirmed original IDs remained, waited for writes, undid deletion, confirmed all four IDs returned, waited for writes, and reloaded; confirmed two Notes/two Frames and absence of retired controls                                                | **Failed content assertion**: the original Note body survived, but the restored copied Note returned empty content with `contentMissing: true`. Final per-node geometry assertions ran only up to that Note; later nodes' geometry checks and the full-success annotation were not reached. All requested UI operations were executed, not all outcomes accepted. |
| Isolation evidence points to this worktree and disposable storage        | Verified workspace registry path, matching on-disk World identity and API identity, and both server processes' current working directories using lsof                                                                                                                                                                                                                                                                                                                      | **Passed**                                                                                                                                                                                                                                                                                                                                                        |

Across the latest run, the browser audit recorded **324 API requests** (154 lifecycle, 99 geometry, 68 ordinary workflow, 3 isolation), zero legacy references endpoint requests, zero `SET_PORTAL_NODE_PINS` requests, and zero forbidden external/Agent requests. No `pageerror` was recorded; the failed error audits contain seven HTTP 400 responses and seven corresponding browser console resource errors. The ordinary and isolation browser audits contain no errors. Controls were checked on the exercised surfaces, not exhaustively across all application dialogs.

## Confirmed current-runtime defect

The web preprocessing queue sends `POST /api/canvas/:worldId/nodes/:previewId/preprocess` with `{"nodeType":"spacePreview","trigger":"node_updated","snapshot":{}}`. The actual server responds **400** with `Invalid option: expected one of "note"|"text"|"web"|"pdf"|"office"|"image"|"video"|"frame"|"question"`.

The queue's [schedule guard](../src/store/canvasStore/save/preprocessQueue.ts#L101-L111) excludes `sketch` but not `spacePreview`; the [shared preprocessing schema](../../../packages/shared/src/types/api/preprocessing.ts#L44-L54) excludes `spacePreview`. This is consistent with the captured request/response mismatch. The behavior was reproduced in earlier runs and both follow-up runs. A read-only code comparison against HEAD `ff7f4e45222fee7e6e78853533f52eb2619d44d6` confirmed that the same schedule guard and schema exclusion already exist there; the four inspected preprocessing-path files have no diff against HEAD. **No baseline browser run was performed**, so this does not establish identical baseline runtime behavior or attribute the defect to the uncommitted retirement. No 400 was ignored, preprocessing was not mocked, and no production fix was attempted.

## Additional runtime blocker exposed after the selection fix

The ordinary workflow now reaches delete/undo/reload. In both follow-up runs, the copied Note's content existed before deletion but was missing after undo and reload. Run 2 explicitly polled both Note bodies before deletion and waited for real mutation settling before deletion, before undo, and before reload; it still reproduced the loss. Undo restored the four topology IDs, not the copied Note's persisted body.

The final content attachment for copied Note `node-473aa4c3-b25a-4d75-944c-5319f1342148` reports `{"content":"","contentMissing":true}` with HTTP 200. The original Note still contains `Copy and undo marker`. The trace additionally records HTTP-200 preprocessing results with `success:false` and `error:"Node sidecar is missing"` for both restored copied nodes (Note and Frame). The ordinary roundtrip screenshot visibly shows the original content and two missing-file banners. These application-level failures do not produce an HTTP-error audit entry; the strict content assertion correctly keeps the ordinary test red. The deeper cause and HEAD runtime behavior were not established, and no production history/storage fix or test-side file recreation was attempted.

## Earlier iterations (historical, superseded by the follow-up below)

1. Run 1: 0 passed / 4 failed. The test initialization accessed localStorage on an opaque initial document, and it assumed the older modal Note editor used by an existing E2E suite. Corrected the initialization guard and current tabbed editor selectors.
2. Run 2: 1 passed / 3 failed. World lifecycle functional checks completed and exposed the 400 defect. Immediate unload raced pending save work; local viewport reload preceded its 150 ms persistence debounce; the ordinary workflow needed explicit Select mode. Added mutation-settling and localStorage assertions, without suppressing errors.
3. Run 3: 1 passed / 3 failed. Both World functional workflows completed; ordinary Frame creation started over selected-node chrome. Added the existing harness's pending-tool readiness assertion and moved the gesture to empty canvas.
4. Run 4: 1 passed / 3 failed. Both World functional workflows again completed. Ordinary Frame creation and movement completed, but the unsupported/unhandled full-canvas select-all assumption blocked the remaining workflow. Stopped after three rounds of selector/timing corrections rather than applying more speculative fixes. A follow-up should use the existing mouse suite's verified marquee selection path, then execute the remaining clipboard/history/reload checks; this has **not** been done.

Before this follow-up, scoped ESLint exited 0 with 30 non-null-assertion warnings, and the standalone TypeScript check found TS2769 and TS7006 in the union-typed `page.on(event, ...)` listener loop despite clean editor diagnostics. Those results are historical, not the current validation status.

## Authorized script-only follow-up

1. Correction/run 1: registered `requestfinished` and `requestfailed` separately with a shared `Request`-typed callback and `Set<Request>`; replaced browser select-all with the existing mouse suite's real marquee gesture; added selected-count and persisted-deletion identity assertions; replaced non-null assertions in the touched request, drag, and config paths with runtime guards. All four cases ran: 1 passed / 3 failed in 90.84 seconds. Both World workflows completed; ordinary copy/paste, delete, undo and reload executed, exposing missing restored content rather than a selection blocker.
2. Correction/run 2: added persisted Note-body checks before deletion and mutation-settling boundaries before deletion and undo; attached reloaded content responses and a screenshot, and recorded an executed-steps annotation separately from the full-success annotation. All four cases ran again: 1 passed / 3 failed in 98.45 seconds. The same two World audit failures and ordinary restored-content failure remained. Stopped after **two** correction rounds, below the authorized three-round maximum; no speculative third script change can justify weakening the observed content failure.

Final scoped **strict TypeScript passes**, editor diagnostics show no errors, and scoped Prettier checks pass. Scoped ESLint exits 0 with **0 errors and 23 remaining non-null-assertion warnings**, down from 30; untouched assertions were not churned solely to clear warnings. No `any`, TypeScript suppressions, ignored 400s, expected-failure marking, or preprocessing mocks were added. Production typechecks, root formatting and root lint-fix were not run.

## Isolation and cleanup proof

- Backend: `http://127.0.0.1:3197`, PID **21979** during the latest run, cwd `/Users/christy/Documents/code/Huabu/apps/server`.
- Frontend: `http://127.0.0.1:5297`, PID **21989** during the latest run, cwd `/Users/christy/Documents/code/Huabu/apps/web`; its API proxy was explicitly `http://127.0.0.1:3197`.
- Workspace: `/var/folders/qz/cqzsh0xs0z1g92t3ty9t2f5w0000gn/T/huabu-e2e-workspace-21885-c132677f-9b0a-40ac-b3d1-aa4a0a7b56dc`.
- Data/HOME: `/var/folders/qz/cqzsh0xs0z1g92t3ty9t2f5w0000gn/T/huabu-e2e-data-21885-c132677f-9b0a-40ac-b3d1-aa4a0a7b56dc`.
- Workspace ID: `1387c9f4-a208-489e-8025-8575717c54b7`; World ID: `canvas-005c1f92-7c87-4412-9fae-804201e092a8`.
- Both services had `reuseExistingServer: false`. lsof verified the actual service directories, not just the intended shell cwd. After teardown both temporary directories were absent, both final service PIDs were absent, and neither test port had a listener. No test server remains running.
- Round 1 cleanup was also verified before round 2: PIDs **18285** and **18299** were absent, its workspace/data paths ending `18118-63dc9e64-d305-497b-a863-b19ee05b74f6` were absent, and neither isolated test port had a listener.
- Final `ps -p` checks returned exit 1 for both latest service PIDs; `lsof -nP -iTCP:5297 -iTCP:3197 -sTCP:LISTEN` returned no listeners; filesystem existence checks returned false for both latest temporary directories. No service on 5294 or real workspace was used, stopped, or reconfigured by this follow-up.
- The already-installed Playwright browser cache was reused. Test reports, traces and screenshots are retained as evidence. No real workspace data or credential store was edited.

## Commands executed

Historical browser installation (not repeated in this follow-up):

```sh
pnpm --dir /Users/christy/Documents/code/Huabu/apps/web exec playwright install chromium
```

The two follow-up executions used the command below, with redirected logs `/tmp/huabu-portal-script-fix-run1.log` and `/tmp/huabu-portal-script-fix-run2.log`. Automatic retries were disabled. Run 1 report and artifacts were retained at `/tmp/huabu-portal-script-fix-report-run1` and `/tmp/huabu-portal-script-fix-artifacts-run1` before run 2 replaced the standard output directories. Earlier logs `/tmp/huabu-portal-e2e-run1.log` through `/tmp/huabu-portal-e2e-run4.log` belong to the historical acceptance rounds.

```sh
pnpm --dir /Users/christy/Documents/code/Huabu/apps/web exec playwright test --config playwright.portal-retirement.config.ts
```

Scoped checks, from the Huabu root unless `--dir` is provided:

```sh
pnpm exec prettier --write apps/web/playwright.portal-retirement.config.ts apps/web/e2e/portal-retirement.spec.ts
pnpm exec eslint apps/web/playwright.portal-retirement.config.ts apps/web/e2e/portal-retirement.spec.ts
pnpm --dir /Users/christy/Documents/code/Huabu exec prettier --check apps/web/playwright.portal-retirement.config.ts apps/web/e2e/portal-retirement.spec.ts
pnpm --dir /Users/christy/Documents/code/Huabu/apps/web exec tsc --ignoreConfig --noEmit --skipLibCheck --esModuleInterop --strict --target es2022 --module esnext --moduleResolution bundler --types node playwright.portal-retirement.config.ts e2e/portal-retirement.spec.ts
```

The first standalone tsc invocation omitted `--ignoreConfig` and returned TS5112 under TypeScript 6; the command above reached the two actual spec diagnostics.

## Evidence and changed files

- [HTML report](../playwright-report/portal-retirement/index.html) and [JSON results](../playwright-report/portal-retirement/results.json): final statuses, functional-completion annotations, all browser-audit request/response bodies, and the isolation-proof attachment.
- [Live source update screenshot](../test-results/portal-retirement/portal-retirement-World-me-3c21c-esh-and-test-Space-deletion-touch/world-live-source-update.png): visually inspected; shows the updated Note excerpt in World.
- [Persisted preview screenshot](../test-results/portal-retirement/portal-retirement-World-pr-3da32-nd-local-viewport-isolation-touch/preview-persisted-geometry.png): visually inspected; shows the resized preview and the locally panned/zoomed, intentionally clipped excerpt. Persistence claims additionally depend on the recorded backend/CSS/viewBox assertions, not the screenshot alone.
- [Ordinary workflow roundtrip screenshot](../test-results/portal-retirement/portal-retirement-ordinary-b8191-py-paste-delete-undo-reload-touch/ordinary-frame-note-roundtrip.png): visually inspected in this follow-up; shows four restored nodes after reload, with original content retained and missing-file warnings on the copied pair, not the former page-text-selection blocker.
- Per-test trace ZIPs, failure videos and screenshots are under [test-results/portal-retirement](../test-results/portal-retirement); the HTML report provides direct attachment access.
- Follow-up changed files only: [focused config](../playwright.portal-retirement.config.ts), [focused spec](portal-retirement.spec.ts), and this report. These task-owned files were already untracked when the follow-up began. Existing uncommitted Portal/Pin retirement production files were not edited, and no commits were created.

**Remaining blockers:** the preview preprocessing 400 and restored copied-node content loss prevent acceptance. Script listener/selection issues are resolved; all requested ordinary UI steps now execute. Any production preprocessing or history/storage investigation and fix requires separate authorization and is outside this test-only change.
