{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "node16",
    "moduleResolution": "node16",
    "target": "ES2020",
    "outDir": "dist",
    "rootDir": "src",
    "types": ["node"]
  },
  "include": ["src"],
  // Tests are run by vitest, never emitted into `dist/` (and therefore never
  // shipped inside app.asar). Excluding them also lets a test import the
  // server's secret-id contract for parity checking without violating
  // `rootDir`.
  "exclude": ["src/**/*.test.ts"]
}
